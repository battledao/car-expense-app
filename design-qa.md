# V1.21 首页最近记录 Design QA

> 验收日期：2026-09-17
> 验收范围：首页“最近记录”模块、记录详情交互、响应式布局与跨模块回归

## 视觉基准与实现证据

- 参考模板：`C:\Users\admin\AppData\Local\Temp\codex-clipboard-b8b17321-2130-425a-b2d3-f4fb1554fee6.jpg`（1290 × 945）。
- 实现聚焦图：`C:\Users\admin\AppData\Local\Temp\v21-recent-panel.png`（350 × 497）。
- 移动端全页：`C:\Users\admin\AppData\Local\Temp\v21-dashboard-mobile.png`（430 × 1928）。
- 桌面端全页：`C:\Users\admin\AppData\Local\Temp\v21-dashboard-desktop.png`（1280 × 1793）。
- 并排对照图：`C:\Users\admin\AppData\Local\Temp\v21-recent-comparison.png`（1071 × 524）。
- 浏览器 CSS 视口：430 × 932、1280 × 900；设备像素比 1。
- 聚焦状态：全部车辆、2026-09、5 条最近记录、详情面板关闭。

## 对照结论

- 模块标题与“查看全部”保持同一行，标题、入口、分类、金额和时间形成稳定的信息层级。
- 单条记录采用“分类图标 / 分类与车辆 / 金额与时间”三列结构；金额右对齐，日期时间使用 `MM-DD HH:mm`。
- 全部车辆模式显示车辆名；具体车辆模式隐藏重复车辆名。
- 首页仅显示最新 5 条记录，并以稳定规则处理时间相同的记录；不受首页统计月份限制。
- 列表行保持至少 44px 可点击高度，长车辆名与大额金额在窄屏下不会互相覆盖。
- 移动端页面顺序已调整为关键指标、最近记录、费用构成；430px 实测最近记录顶部为 712px，费用构成顶部为 1259px。
- 参考模板的绿色渐变图标改为应用既有的青、蓝、橙、粉分类色；这是为了维持全站视觉一致性。
- 参考模板仅展示 3 条，当前实现按 V1.21 展示 5 条并压缩垂直间距；信息密度提高但仍保持清晰分隔。

## 交互与可访问性

- 鼠标点击、Enter、Space 均可打开详情；Escape 关闭详情并将焦点返回原记录。
- 编辑、复制、删除沿用完整记录能力；删除后焦点移至下一条或空状态入口。
- “查看全部”进入干净的 `/records` 路径，并保留具体车辆上下文。
- 已覆盖空状态、全部车辆、具体车辆、长文本、大金额、同时间记录、放大文字及横屏。
- 检查宽度：320、375、390、414、430、767、768、1280，以及 844 × 390 横屏。
- 视觉复测控制台错误：0；横向溢出：0。

## 迭代记录

1. 第一轮发现“最近记录”仍位于“本月费用构成”之后，与 V1.21 移动端信息优先级不符。
2. 调整首页内容顺序，并重新运行组件测试和首页端到端测试。
3. 第二轮重新生成移动端、桌面端及聚焦对照图；未发现 P0、P1 或 P2 视觉问题。

## 工具说明

应用内 Browser 控制通道调用失败，错误为 `Browsers: Error: nodeRepl.fetch request failed`。本轮按既定回退方案使用项目 Playwright 配置中的本机 Chrome 完成真实浏览器验证和截图，并通过图片检查工具逐张复核。

final result: passed
