# 车主用车记账 App 产品需求文档（PRD）

> 产品名称：暂定“爱车小精灵”  
> 文档版本：V1.10
> 状态：第一版需求已确认；V1.10 “更多”模块 UI 优化需求已确认
> 更新日期：2026-09-08
> 适用范围：电脑与手机浏览器本地 Web 版

## 版本变更记录【V1.10 修改】

| 版本 | 日期 | 修改范围 | 修改说明 |
| --- | --- | --- | --- |
| V1.0 | 2026-08-30 | 第一版整体需求 | 建立产品目标、页面结构、模块范围、本地数据、备份恢复及验收标准。 |
| V1.1 | 2026-08-30 | 导航、用车记账、详细记录和本地存储说明 | 补充导航图标、油品标号下拉、详细记录默认排序与清除筛选规则，以及跨浏览器数据迁移边界。 |
| V1.2 | 2026-08-31 | 5.6 能耗统计、AC-17～AC-20 | 重新设计能耗统计的信息结构、计算规则、趋势、成本、数据质量、补能记录管理和验收标准；建立版本及正文变更标记规则。 |
| V1.3 | 2026-09-02 | 第 3 章、5.1、6.3、第 8～9 章、AC-09～AC-10、AC-36～AC-42 | 重新设计首页总览的信息层级、月份条件、分车辆视图、核心指标、费用趋势、类别构成、车辆与能耗摘要、最近记录、快捷操作、数据提醒、空状态、响应式交互及验收标准；完整记录首页暂时不做的内容。 |
| V1.4 | 2026-09-02 | 5.2、6.5、第 8～9 章、AC-04～AC-08、AC-15、AC-25、AC-43～AC-50、11.2 | 重新设计用车记账的页面流程、车辆与场景选择、分车型适配、加油与充电差异化字段、自动计算、里程提示、表单校验、保存反馈、继续记账、编辑清理和模块联动；完整记录本轮暂时不做的内容。 |
| V1.5 | 2026-09-03 | 5.3、第 8～9 章、AC-11、AC-51～AC-58 | 重新设计费用日历的页面结构、月份和车辆条件、月度摘要、日期格、当天明细、记录操作、日历记账入口、空状态、响应式与键盘交互及验收标准；完整记录本轮暂时不做的内容。 |
| V1.6 | 2026-09-04 | 文档范围、5.4、第 8～10 章、AC-12、AC-59～AC-67 | 重新设计数据分析的页面结构、统一筛选、核心指标、费用趋势、月份对比、类别与车辆构成、明细下钻、空状态、联动及移动端响应式要求；明确本地 Web 版支持电脑与手机浏览器。 |
| V1.7 | 2026-09-04 | 5.5、第 8～10 章、AC-13～AC-16、AC-34、AC-47、AC-68～AC-80 | 重新设计详细记录的页面结构、筛选与 URL 状态、结果汇总、桌面表格与手机卡片、详情、编辑、删除、复制、加载更多、跨模块联动、空状态及可访问性要求。 |
| V1.8 | 2026-09-06 | 5.7、6.6、第 8～10 章、AC-02～AC-03、AC-22、AC-81～AC-94 | 重新设计车辆管理的页面结构、车辆摘要卡片、新增与编辑校验、默认车辆规则、数据敏感字段保护、跨模块快捷入口、级联删除安全、空状态、移动端响应式及可访问性要求。 |
| V1.9 | 2026-09-07 | 1.4、第 2～3 章、5.2、5.4、5.7、第 8～10 章、AC-01、AC-33、AC-95～AC-108 | 将首页、用车记账、数据分析和车辆管理四张已确认视觉稿共同确定为正式视觉基准；建立移动优先的响应式导航、颜色、字体、间距、圆角、阴影、组件状态、图表、插画、可访问性、性能和视觉回归验收规范。 |
| V1.10 | 2026-09-08 | 2.2～2.3、5.8、6.7、第 8～10 章、AC-01、AC-97、AC-109～AC-118 | 重新设计手机端“更多”模块，将车辆管理、能耗统计和数据管理组织为具有真实状态摘要的独立入口卡片；补充顶部上下文、空状态、路由闭环、响应式、可访问性、数据安全和上线约束。 |

变更标记规则：

- `【Vx.x 新增】`：该版本新加入的章节或内容；
- `【Vx.x 修改】`：该版本调整过的章节或内容；
- 删除内容不保留在正文中，只在对应版本的变更记录中说明。

## 1. 产品目标和使用场景

### 1.1 产品目标

为个人车主提供一个专门记录和分析用车支出的本地 App，让用户能够：

- 管理多辆车，并明确每笔记录属于哪辆车；
- 快速记录加油、充电、停车、保养、保险等实际用车费用；
- 从日期、费用类别、车辆和时间趋势等角度查看支出；
- 分别了解燃油车、纯电动车和插电式混动车的油耗、电耗及能源成本；
- 在不登录、不联网的情况下长期保存数据；
- 通过导出和导入备份文件保护及恢复本地数据。

第一版的重点是“记录方便、数据清楚、本地可靠”，不追求社交、联网或复杂自动化能力。

### 1.2 目标用户

- 拥有一辆或多辆汽车的个人车主；
- 希望了解日常养车成本，但不想使用通用财务软件的用户；
- 希望所有数据只保存在自己电脑上的用户；
- 使用燃油车、纯电动车或插电式混动车的用户。

### 1.3 主要使用场景

1. 用户加油、充电或产生停车等费用后，快速记一笔。
2. 用户回顾某一天或某个月发生过哪些用车费用。
3. 用户查看本月总费用、主要费用类别及近期支出变化。
4. 用户比较不同月份或不同车辆的用车成本。
5. 用户通过带里程的补能记录查看车辆油耗、电耗和每公里能源成本。
6. 用户新增、切换或维护多辆车的资料。
7. 用户定期导出完整备份，并在数据异常、浏览器数据被清理或更换环境时导入恢复。

### 1.4 第一版使用环境【V1.9 修改】

- 在用户自己的电脑或手机浏览器中本地运行；
- UI 设计和视觉验收以手机端为第一优先级，同时保证桌面端 Chrome、Edge 等 Chromium 浏览器可正常使用；
- 手机端重点覆盖 320px、375px、390px、414px 和 430px 宽度，并验证常见 iPhone 竖屏、横屏及底部安全区域；
- 桌面端重点覆盖 1024px、1280px、1440px 及以上宽度；
- 不要求登录、联网、服务器、云数据库或部署；
- 默认使用人民币、公里、升和千瓦时作为金额及计量单位；
- 刷新页面、关闭浏览器和重启设备后，已保存数据必须仍然存在。

## 2. 整体页面框架和导航结构【V1.9 修改】

### 2.1 响应式页面框架【V1.9 修改】

第一版采用移动优先的响应式结构。不同宽度只调整导航和布局，不得删除功能、改变数据口径或形成两套业务逻辑。

```text
手机端（≤767px）                         桌面端（≥1024px）
┌──────────────────────┐                ┌────────────┬──────────────────────┐
│ 页面标题 / 上下文操作 │                │ 左侧主导航  │ 顶部全局栏            │
│                      │                │            ├──────────────────────┤
│ 当前页面内容          │                │            │ 当前页面内容          │
│                      │                │            │                      │
├──────────────────────┤                │            │                      │
│ 首页 日历 记录 分析 更多│               └────────────┴──────────────────────┘
└──────────────────────┘
```

断点规则：

- `≤767px`：使用手机端单列内容和底部五项导航；
- `768～1023px`：使用双列内容或单列内容，并根据可用宽度采用紧凑导航栏；同一宽度下保持导航位置稳定，不因页面不同而跳变；
- `≥1024px`：保留左侧主导航、顶部全局栏和中间内容区；
- 页面内容需适配浏览器安全区域、软键盘、横竖屏和文字放大；任何断点下页面主体不得产生整体横向滚动。

### 2.2 主导航【V1.9 修改】

桌面端左侧导航固定包含七个一级入口：

1. 首页总览
2. 用车记账
3. 费用日历
4. 数据分析
5. 详细记录
6. 能耗统计
7. 车辆管理

各模块必须解决不同问题，不允许仅复用同一任务列表并更换标题。

为提升扫描效率，每个一级导航名称左侧应显示与模块场景匹配的简洁图标；图标与文字共同构成可点击导航项，不能只依赖颜色区分当前页面。

手机端底部导航固定包含五个入口，顺序为：

1. 首页；
2. 费用日历；
3. 详细记录；
4. 数据分析；
5. 更多。

手机端“更多”页集中提供能耗统计、车辆管理和数据管理入口；三个入口必须按 V1.10 规定显示为边界和用途清楚的独立卡片，不得使用相互粘连的普通文字链接。“用车记账”通过首页主按钮、页面内快捷入口和各记录场景入口进入，不在“更多”页重复设置悬浮新增按钮。底部导航必须同时显示图标和文字，当前项使用形状、字重或底色与颜色共同标识。进入用车记账、编辑记录、车辆新增/编辑等专注任务时隐藏底部导航，改用页面返回和固定操作区，避免误触离开。【V1.10 修改】

### 2.3 全局栏与页面级上下文【V1.9 修改】

桌面端顶部全局栏包含：

- 当前车辆选择器；
- “全部车辆”选项；
- “记一笔”快捷按钮；
- “数据管理”入口，用于导出备份和导入恢复；
- 本地保存状态，例如“正在保存”“已保存”“保存失败”。

选择车辆后，首页、费用日历、数据分析和详细记录应同步应用该车辆条件。能耗统计只能针对一辆具体车辆；从“全部车辆”进入时，应提示选择车辆。

手机端不强制保留完整顶部全局栏，车辆、月份、筛选和主要操作按当前页面放入标题下方或首个内容区；数据管理收拢到“更多”。“更多”页不显示全局车辆选择器和单独“+”按钮，顶部只保留页面标题、副标题及确有必要的页面级状态，避免与管理入口争夺注意力。全局车辆条件仍必须跨首页、费用日历、数据分析和详细记录保持一致；从“更多”进入能耗统计时按 5.8 的规则读取当前车辆状态。本地保存状态以不遮挡内容的轻提示、状态文字或失败提醒呈现，不得长期占据手机首屏。【V1.10 修改】

### 2.4 统一交互形式

- **独立页面**：用于浏览首页、日历、分析、记录列表、能耗和车辆管理等大量内容；
- **右侧抽屉**：用于查看或编辑单条记录，关闭后保留用户原来的页面、筛选和滚动位置；
- **确认弹窗**：只用于删除、覆盖导入等高风险操作；
- **即时反馈**：新增、修改、删除或导入后，给出明确的成功或失败反馈；
- **离开提醒**：表单存在未保存修改时，关闭抽屉或离开页面需提醒用户。

### 2.5 正式视觉基准【V1.9 新增】

首页、用车记账、数据分析和车辆管理四张于 2026-09-07 确认的视觉稿共同构成本项目正式视觉基准。后续页面应复用相同的视觉语言，不得逐页采用互不一致的配色、圆角、阴影或图标体系。

视觉稿规定视觉层级、布局节奏和组件风格，不改变既有功能和数据规则。视觉稿中的金额、日期、里程、车牌、车型、趋势和记录均为示意内容，产品运行时必须使用真实数据；不得把整张视觉稿作为背景图片，也不得用图片替代真实文字、按钮、输入框或图表。

发生冲突时按以下优先级处理：数据正确与安全 > 已确认业务需求 > 可访问性和可操作性 > 正式视觉基准的视觉还原。任何因屏幕宽度、文字放大或数据长度导致的适配，都应优先保证内容完整和操作可用。

### 2.6 设计令牌【V1.9 新增】

#### 2.6.1 颜色

| 用途 | 基准值 | 使用要求 |
| --- | --- | --- |
| 页面背景 | `#F4F8F9` | 全局冷雾灰白背景，不使用大面积纯白造成层级缺失。 |
| 主表面 | `#FFFFFF` | 卡片、输入区和底部导航的基础表面。 |
| 半透明表面 | `rgba(255, 255, 255, 0.88)` | 仅作增强效果；关闭模糊或低性能设备上仍需清楚可读。 |
| 主文字 | `#102235` | 标题、金额和重要正文。 |
| 次文字 | `#5E6C7B` | 说明、单位、日期和辅助信息。 |
| 主操作深青 | `#08766F` | 主按钮背景、强调状态；按钮文字使用白色。 |
| 品牌青绿 | `#0C8F86` | 选中、高亮、趋势线和非小字号强调文字。 |
| 辅助天蓝 | `#2F80ED` | 里程、信息类图标和辅助数据，不与主操作竞争。 |
| 分隔线 | `#DCE6E9` | 卡片边框、列表分隔和图表网格。 |
| 警告 | `#9A5A00` | 数据不足或需要注意，同时配合文字或图标。 |
| 危险 | `#B42318` | 删除和严重错误，同时配合文字、图标或边框。 |
| 焦点 | `#1D70B8` | 键盘焦点轮廓，不能被阴影或裁切隐藏。 |

实现时允许为对比度和设备显示微调颜色，但必须集中维护为设计令牌，不得在页面内散落近似色。普通文字与背景对比度至少达到 `4.5:1`，大号文字和关键 UI 图形至少达到 `3:1`；状态不得只靠颜色表达。

#### 2.6.2 字体与数字

- 字体族使用 `-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Microsoft YaHei", sans-serif`，不依赖远程字体；
- 核心金额：40/48px、700；页面一级标题：28/36px、700；区块标题：22/30px、700；卡片标题：18/26px、600；正文：16/24px、400；辅助文字：14/20px、400；最小说明文字：12/18px、400；
- `320px` 宽度或文字放大时，核心金额可降至 34px，但不得截断、重叠或缩小正文至 12px 以下；
- 金额、里程、百分比和图表数值使用等宽数字特性 `font-variant-numeric: tabular-nums`；金额统一保留两位小数并正确显示人民币符号，单位与数值之间保持稳定间距；
- 页面必须支持浏览器 200% 文字缩放；文字放大后允许卡片增高和按钮换行，不允许裁掉关键数据。

#### 2.6.3 间距、尺寸与布局

- 基础间距采用 `4、8、12、16、20、24、32px` 序列；移动端页面左右留白 20px，320px 宽度下可降为 16px；区块间距默认 24px；
- 输入框和常规按钮可视高度不低于 48px，所有触控目标至少约 `44×44px`；相邻危险与普通操作之间必须保留足够间隔；
- 控件圆角 12px，内容卡片圆角 18px，胶囊标签使用全圆角；同类组件不得出现无规则圆角差异；
- 关键卡片使用 `0 6px 20px rgba(16, 34, 53, 0.08)` 的柔和阴影和 1px 浅色边框；普通列表优先使用边框或分隔线，避免每个元素都叠加阴影；
- 手机端底部导航和固定操作区必须增加 `env(safe-area-inset-bottom)`；顶部内容需兼容 `env(safe-area-inset-top)`；
- 桌面内容区设置合理最大宽度并居中或对齐导航，不把手机布局等比放大成超宽卡片。

### 2.7 通用组件与状态【V1.9 新增】

- **按钮**：定义主按钮、次按钮、文字按钮和危险按钮；每类均需实现默认、悬停、按下、键盘焦点、加载、禁用和失败后的可恢复状态；加载时保留按钮宽度并阻止重复提交；
- **输入与选择器**：实现默认、聚焦、已填写、只读、禁用和错误状态；标签不能只依赖输入框占位符；错误紧邻字段并说明修正方法；
- **卡片**：使用标题、主要数值、辅助信息和操作的稳定层级；整卡可点击时必须有明确焦点样式，不得与卡内独立按钮产生冲突；
- **标签与分段控件**：选中状态至少同时改变背景或边框、字重或图标，不能只改变文字颜色；选项过多时允许组件内部横向滚动，但必须有可发现的更多入口且不引起整页横向滚动；
- **图表**：真实绘制并使用真实数据；提供标题、单位、坐标或标签、当前点状态和等价文字数据；加载、无数据、数据不足和计算失败分别处理，不用空白图或 `0` 冒充错误；
- **反馈**：成功提示简短且不阻塞后续操作；错误保留用户输入并提供重试或修正入口；高风险操作继续使用确认弹窗；
- **加载与空状态**：骨架屏形状与最终内容接近，避免布局跳动；空状态说明当前原因和一个明确下一步，不展示与当前条件无关的装饰性零值；
- **图标**：统一使用同一套线性图标风格，常规尺寸 20～24px、线宽约 1.75～2px；不得使用 emoji 或字体字符冒充产品图标；所有仅图标按钮必须有可访问名称。

### 2.8 插画、动效、性能与安全【V1.9 新增】

- 首页和车辆卡片可使用低对比度车辆或城市剪影作为装饰，但必须与文字和操作分层，不降低内容对比度，也不承载唯一业务信息；
- 装饰插画应拆分为独立本地资源，优先使用经过授权且体积可控的 SVG、WebP 或 PNG；不得依赖运行时远程图片，不得包含真实车牌、用户数据、Secret、内部地址或文字；
- 装饰资源使用 `aria-hidden="true"` 或等效方式从辅助技术中隐藏；功能图标不得按装饰资源处理；
- 动画时长控制在 160～240ms，以状态反馈和层级过渡为主；不得使用持续循环、妨碍阅读或阻塞操作的动画；系统开启减少动态效果时应关闭非必要动画；
- 首屏装饰资源不得阻塞核心文字和操作渲染；慢设备或资源加载失败时，移除装饰后页面仍可完整使用；
- 本轮视觉改造不得新增网络服务、系统权限、追踪 SDK、远程字体或包含 Secret 的配置；引入第三方图标或插画前必须检查维护状态、License、隐私和 App Store 兼容性。

## 3. 首页的内容结构【V1.9 修改】

首页用于让车主打开 App 后快速了解：当前月份花了多少钱、相比上月如何变化、钱主要花在哪里、当前车辆最近发生了什么、车辆的里程和能耗情况如何，以及是否存在需要补充或处理的数据。首页以概览和快捷跳转为主，不承担复杂分析和批量管理。

### 3.1 页面条件与顶部操作【V1.3 修改】

- 首页同时受顶部全局车辆条件和页面月份条件控制；
- 车辆条件支持具体车辆和“全部车辆”，切换后首页全部内容立即重新计算；
- 月份默认当前月，支持上个月、下个月和“回到本月”；
- 页面显示当前车辆名称或“全部车辆”、能源类型和当前统计月份；
- 提供“记一笔”快捷入口，并自动带入当前具体车辆；
- 燃油车显示“记录加油”，纯电动车显示“记录充电”，插混车同时显示“记录加油”和“记录充电”；
- 快捷补能入口自动带入当前车辆和对应费用类别。

### 3.2 核心指标【V1.3 修改】

具体车辆模式至少显示：

- 本月总费用；
- 本月记录数；
- 平均每笔费用；
- 本月单公里综合用车成本；
- 与上月相比的费用金额差和变化比例。

全部车辆模式至少显示：

- 全部车辆本月总费用；
- 本月总记录数；
- 平均每笔费用；
- 本月有记录的车辆数；
- 与上月相比的费用金额差和变化比例。

计算与显示规则：

- 本月总费用为统计月份内符合当前车辆条件的全部记录金额之和；
- 平均每笔费用为本月总费用除以本月记录数；
- 月度变化同时显示本月与上月的金额差和百分比；上月为零时只显示金额变化，不显示无意义的百分比；
- 单公里综合用车成本为本月全部用车费用除以本月有效行驶里程；
- 只有至少两个有效且递增的里程点时才计算单公里成本；
- 数据条件不足时显示“数据不足”及具体原因，不能显示误导性的 `¥0.00/km`；
- 全部车辆模式不跨车辆合并计算单公里成本或能耗；
- 点击本月费用或记录数进入详细记录，并自动带入当前车辆和月份条件。

### 3.3 最近六个月费用趋势【V1.3 修改】

- 使用折线图或柱状图展示当前统计月份及之前五个月的费用；
- 每个月显示总费用，当前统计月份需突出显示；
- 无费用月份可以显示为零，但必须与数据加载失败或数据不足区分；
- 鼠标悬停或键盘聚焦数据点时，显示月份、总费用和记录数；
- 点击某个月进入详细记录，并自动带入当前车辆和对应月份；
- 全部车辆模式显示全部车辆的月度合计趋势。

### 3.4 本月费用构成【V1.3 修改】

- 按费用类别显示类别图标、名称、金额和占本月总费用的比例；
- 默认按类别金额从高到低排列；
- 默认展示主要类别，并提供“查看全部类别”入口；
- 点击类别进入详细记录，并自动带入当前车辆、当前月份和该类别；
- 本月没有记录时显示“本月暂无费用记录”，并提供“记一笔”。

### 3.5 车辆与能耗摘要【V1.3 修改】

具体车辆模式显示：

- 车辆名称和能源类型；
- 当前记录里程；
- 最近一次记录日期；
- 累计记录数和累计费用；
- 燃油车的最近完整区间油耗和近十二个月平均油耗；
- 纯电动车的最近完整区间电耗和近十二个月平均电耗；
- 插混车的最近油耗、最近电耗和综合能源成本，不生成“综合能耗”数值；
- 能耗数据不足时说明缺少的记录条件；
- 点击能耗摘要进入当前车辆的能耗统计页。

全部车辆模式不混合计算不同车辆的里程和能耗，改为显示车辆费用对比：

- 每辆车本月费用；
- 每辆车本月记录数；
- 每辆车占全部车辆本月总费用的比例；
- 默认按本月费用从高到低排列；
- 点击某辆车后，将其设为当前车辆并切换为该车辆的首页视图。

### 3.6 最近记录【V1.3 修改】

- 默认显示最近五条符合当前车辆条件的记录；
- 每条显示日期和时间、车辆、费用类别、商家或地点及金额；
- 加油或充电记录可补充显示加油量或充电量；
- 具体车辆模式可以弱化车辆名称，全部车辆模式必须清楚显示车辆名称；
- 点击记录后在右侧抽屉查看完整信息，并提供“编辑记录”入口；
- 提供“查看全部记录”，进入详细记录时保留当前车辆条件。

### 3.7 数据提醒与处理入口【V1.3 新增】

- 首页只显示需要用户处理的主要问题，不展示完整数据质量报表；
- 提醒范围包括能耗计算缺少里程、补能量缺失、尚未形成完整加满或充满区间、里程重复或倒退，以及当前车辆本月没有记录；
- 每条提醒必须提供明确入口，例如“补充记录”“查看补能记录”“进入能耗统计”或“记录第一笔费用”；
- 没有需要处理的问题时，不显示提醒区域。

### 3.8 空状态【V1.3 新增】

- 无车辆时显示“添加第一辆车”，不显示无意义的零值指标和空图表；
- 有车辆但完全无记录时显示车辆信息，并提供“记录第一笔费用”以及适用的加油或充电入口；
- 有历史记录但当前月份无记录时，本月费用显示 `¥0.00`、记录数显示 `0`，月度趋势和历史最近记录继续显示，平均费用和单公里成本显示“数据不足”；
- 全部车辆没有记录时显示车辆数量，并引导选择车辆或新增记录，不显示空的车辆费用对比图。

### 3.9 响应式与操作要求【V1.9 修改】

- 桌面端优先使用多列指标卡及左右分栏；
- 窄屏浏览器中指标、图表和内容区自动变为两列或单列，页面本身不能产生横向滚动；
- 图表数据点、费用类别、车辆对比和最近记录均可使用键盘操作；
- 上涨、下降和异常状态不能只依靠颜色区分；
- 金额、比例、能耗和“数据不足”状态需清晰区分。

### 3.10 首页正式视觉布局【V1.9 新增】

手机端首页按照正式基准自上而下组织：

1. 品牌标题和提醒入口；
2. 当前车辆选择器和月份选择器；
3. 本月费用主卡，包含本月总费用、统计日期范围、记录数、当前里程和“记一笔”主按钮；
4. 最近六个月费用趋势；
5. 最近记录；
6. 本月费用构成、平均每笔、上月变化、单公里成本、车辆与能耗摘要、快捷补能及数据提醒等既有内容。

首页首屏优先让用户完成“确认车辆与月份—查看本月费用—记一笔”。第 6 项内容允许位于首屏下方，但不得删除、合并为错误口径或降低为无法发现的入口。桌面端可在相同信息顺序基础上使用双栏或多栏，所有指标仍受同一车辆和月份条件控制。

补充视觉规则：

- 本月费用是首页最突出的数值；记录数和当前里程作为同一卡片内的辅助指标，当前里程必须使用既有计算规则；
- 主卡内青色短线仅作为品牌分隔装饰，不表示预算、完成度或可点击进度；在未实现预算功能前不得赋予数据语义；
- 车辆和城市剪影仅为装饰，可根据屏幕空间隐藏；隐藏后金额、日期、指标和主按钮的布局仍完整；
- 提醒图标只承接 3.7 已存在的数据提醒。存在未处理提醒时才显示未读标记；没有提醒时不得伪造红点，也不得扩展为消息推送或到期提醒；
- 趋势图突出当前月份，点选或聚焦后显示月份、金额和记录数；图表下方继续保留等价文字数据；
- 最近记录在手机端至少显示类别、日期和金额；点击后仍按既有要求进入详情，并提供“查看全部记录”；
- 过长车辆名称、五位及以上金额、较大里程和无数据状态均不得破坏主卡布局。

## 4. 每个模块的用途

| 模块 | 主要用途 | 主要回答的问题 |
| --- | --- | --- |
| 首页总览 | 展示当前车辆及全局重要摘要 | 最近花了多少、花在哪里、车辆近况如何？ |
| 用车记账 | 快速新增不同场景的费用或补能记录 | 这次用车产生了什么记录？ |
| 费用日历 | 按日期回顾费用 | 哪一天发生了哪些费用？ |
| 数据分析 | 从趋势、类别和车辆角度分析成本 | 费用如何变化，哪类或哪辆车成本更高？ |
| 详细记录 | 查找、查看和管理完整历史记录 | 某笔记录的完整内容是什么？ |
| 能耗统计 | 统计油耗、电耗和能源成本 | 车辆实际能耗和每公里能源成本是多少？ |
| 车辆管理 | 管理多辆车及车辆基础资料 | 当前管理哪些车，每辆车的基本情况如何？ |

## 5. 各模块第一版最必要的数据和操作

### 5.1 首页总览【V1.3 修改】

**必要数据**

- 当前车辆或全部车辆状态，以及当前统计月份；
- 本月费用、记录数、平均每笔费用、上月对比；
- 具体车辆的本月单公里综合用车成本；
- 最近六个月费用趋势和本月费用类别构成；
- 具体车辆的车辆状态与能耗摘要；
- 全部车辆模式下的车辆费用对比；
- 最近五条记录；
- 主要数据问题及明确处理入口；
- 无车辆、无记录和当前月份无记录等空状态。

**必要操作**

- 切换车辆和统计月份；
- 点击指标、趋势月份或费用类别进入带筛选条件的详细记录；
- 点击全部车辆费用对比项切换到具体车辆；
- 查看最近记录详情并进入编辑；
- 快速记账、记录加油或记录充电；
- 进入全部记录或当前车辆的能耗统计；
- 从数据提醒进入对应处理页面。

### 5.2 用车记账【V1.9 修改】

**模块目标**

- 让用户按“选车—选场景—填内容—保存”的顺序，快速、准确地新增一笔车辆费用或补能记录；
- 高频字段优先、选填字段后置，不同场景只显示相关内容；
- 减少误填、漏填和重复提交，并让补能记录能清楚说明是否具备能耗计算所需数据；
- 保存后立即联动首页、日历、分析、详细记录和能耗统计。

**页面结构**

1. 页面标题和当前记账状态；
2. 所属车辆选择；
3. 记账场景选择；
4. 根据场景变化的表单；
5. 保存操作和结果反馈。

表单主体在电脑浏览器中保持适合阅读的宽度，不将所有输入框横向铺满页面。

**所属车辆**

- 页面顶部明确显示“本次记给哪辆车”；
- 顶部全局已选具体车辆时，进入页面后自动带入该车；
- 全局为“全部车辆”时，优先带入默认车辆；没有默认车辆时要求用户选择；
- 支持在当前页面切换车辆，并显示车辆名称、能源类型和已记录的最高里程；
- 从首页、费用日历等模块进入时，保留入口带入的车辆；
- 无车辆时不显示不可操作的空表单，显示说明及“新增车辆”入口，新增完成后可返回继续记账。

**记账场景与费用类别**

- 补能费用：加油、充电；
- 日常用车：停车、洗车、高速及路桥费、违章罚款；
- 车辆养护：保养、维修、保险、年检、汽车用品；
- 其他：其他费用。

费用类别按上述场景分组展示，配合图标和清晰的选中状态，不仅使用普通下拉框。进入页面时可默认选择“停车”，但不自动填写金额。

车型与补能场景的适配规则：

- 燃油车支持加油，不提供充电场景；
- 纯电动车支持充电，不提供加油场景；
- 插电式混动车同时支持加油和充电；
- 普通费用不受车辆能源类型限制；
- 切换车辆后，当前补能类别不适用时，页面应提示并要求重新选择。

**所有记录的通用必要数据**

- 唯一编号；
- 所属车辆；
- 费用类别；
- 金额，必填且以人民币“元”为单位显示；
- 发生日期和时间，必填；
- 当前里程，普通费用可在不适用时留空；
- 商家或地点，可选；
- 备注，可选；
- 创建时间和最后修改时间。

**通用表单交互**

- 金额是页面中最醒目的输入项；
- 显示该车已有最高里程作为参考，但不自动将参考值当作本次真实里程保存；
- 商家或地点及备注放在次要的“补充信息”区域，但仍能直接找到和填写；
- 普通费用只显示通用字段，不显示或要求填写补能字段；
- 地点字段可按场景显示更具体的文案，例如“停车场或地点”“门店或维修厂”“收费站或路段”。

**加油记录的专用数据和操作**

- 加油量（升），必填；
- 油品标号，可选；使用下拉选择，提供 92 号、95 号、98 号、0 号柴油和“其他”；
- 单价（元/升），可选或自动计算；
- 是否加满；
- 当前里程，必填；
- 加油站或地点及备注，可选。

填写加油量和单价后自动计算总金额；填写总金额和加油量后自动计算参考单价；自动计算结果允许修改。三个数值不一致时，以用户最后主动修改的字段为准重新计算相关字段。金额保留两位小数，加油量和单价允许两位小数。

页面需说明“是否加满”和“当前里程”会影响油耗区间计算；未加满的记录仍保存费用，也可作为完整区间内的补能量参与计算。

**充电记录的专用数据和操作**

- 充电量（kWh），必填；
- 充电方式，可选；使用下拉选择，提供家用交流充电、公共交流慢充、公共直流快充、随车充和“其他”；
- 单价（元/kWh），可选或自动计算；
- 是否充满；
- 当前里程，必填；
- 充电站或地点及备注，可选。

充电量、单价和总金额使用与加油场景相同的双向计算、手动修改和小数精度规则。页面需说明“是否充满”和“当前里程”会影响电耗区间计算；未充满的记录仍可保存并参与完整区间内的累计。

**表单校验和里程提示**

- 必须选择存在的车辆和有效的费用类别；
- 金额必须大于 0，日期和时间不能为空；
- 金额、里程、数量和单价必须是有效数字，且不能为负数；
- 加油必须填写加油量和当前里程，充电必须填写充电量和当前里程；
- 里程低于该车已有最高里程时，保存前提示这可能影响首页单公里成本和能耗统计；用户确认后仍可保存，以支持补录历史记录；
- 错误在对应字段附近显示，明确说明修正方式，并将焦点移到第一个错误字段；
- 校验失败或保存失败时，已填内容保持不变。

**保存操作和反馈**

- 页面提供“保存并查看记录”和“保存并再记一笔”两个主要操作，同时提供取消或返回；
- 保存并查看记录后进入详细记录，新保存的记录应容易识别；
- 保存并再记一笔后停留在当前页面，保留所属车辆和费用类别，清空本次业务数据，发生时间更新为当前时间；
- 保存中显示“正在保存”并暂时禁用保存按钮，连续点击不能产生重复记录；
- 保存成功或失败都有明确反馈；保存失败时不跳转、不清空表单。

**编辑记录**

- 编辑时完整回填原记录，创建时间保持不变，只更新最后修改时间；
- 修改类别后显示对应场景字段；从加油或充电改为普通费用时，保存后清除不再适用的补能数据；
- 在加油和充电之间切换时，旧场景的专用数据不能残留；
- 编辑成功后返回原查看或列表上下文，所有相关统计立即重新计算。

**正式视觉布局【V1.9 新增】**

- 用车记账属于专注任务：手机端使用返回按钮和“记一笔”标题，不显示底部主导航；返回或浏览器后退均需执行未保存内容保护；
- 页面自上而下依次为车辆选择、费用场景、金额、日期与当前里程、场景专用字段、补充信息和保存操作；高频必填内容必须先于选填内容；
- 车辆选择器以紧凑卡片展示车辆名称、能源类型和最高里程参考；切换车辆后的车型适配和数据清理继续执行既有规则；
- 费用场景使用带图标和文字的圆角选择项。手机端首屏优先展示高频场景，其余场景通过明确的“更多类别”展开或组件内部滚动访问，任何类别均不得被隐藏为不可达；
- 金额区域为表单最突出的输入区域，必须保留清晰标签、人民币单位、数值键盘建议和字段级错误，不得只用超大占位符充当标签；
- 日期和当前里程采用清晰的行式输入；里程参考、数据不足和异常提示不得与真实输入值混淆；
- 商家或地点、备注等选填项默认收拢在可发现的“补充信息”区域；展开状态下仍符合键盘顺序和焦点管理要求；
- 手机端底部固定“保存并查看记录”和“保存并再记一笔”两个操作，并适配安全区域与软键盘；固定区不得遮挡最后一个字段、错误说明或浏览器原生控件；
- 两个保存操作必须有明确的主次层级和完整文字，保存中同时禁用，失败后恢复操作且保留全部输入；桌面端可将固定区改为表单末尾操作区；
- 长文本、200% 文字缩放和 320px 宽度下，类别、金额、字段标签和两个保存按钮允许换行或纵向排列，不得截断关键含义。

### 5.3 费用日历【V1.5 修改】

**模块目标**

- 帮助用户按自然日期回顾用车支出，快速了解当前月份的总费用、记录数量、有费用的日期，以及某一天发生的全部费用；
- 支持从具体日期继续新增、查看、编辑和删除记录，并在操作完成后保留日历上下文；
- 以“按日期回顾和处理记录”为核心，不承担费用趋势、类别占比和车辆成本比较等深度分析功能，避免与数据分析重复。

**页面结构**

1. 页面标题、当前车辆和当前月份；
2. 月份切换及快捷记账操作；
3. 当月费用摘要；
4. 月历和选中日期的记录明细。

桌面宽屏优先采用“左侧月历、右侧当天明细”的布局；空间不足时，当天明细排列在月历下方。页面顶部提供“为今天记一笔”快捷入口。

**月份和车辆条件**

- 默认显示当前自然月，支持上个月、下个月、月份选择器直接跳转和“回到本月”；
- “回到本月”只返回当前自然月，不使用含义不清的“今天”作为月份按钮名称；
- 切换月份后重新计算月度摘要和每天数据；原选中日期不属于新月份时清除选择；
- 使用顶部全局车辆条件；具体车辆模式只显示该车记录，“全部车辆”模式汇总所有车辆；
- 切换车辆时保持当前月份；选中日期仍属于当前月份时继续保留，并立即重新计算日历和当天明细；
- 从日历进入新增、详情或编辑后返回时，应保留原车辆、月份、选中日期和页面位置。

**当月摘要**

- 显示当月费用合计、当月记录数量和有费用的天数；
- 数据范围必须同时符合当前车辆和当前月份条件；
- 金额以人民币显示并保留两位小数，有费用天数按自然日期去重；
- 当月没有记录时分别显示 `¥0.00`、`0 笔`和`0 天`，并提供符合当前场景的空状态引导；
- 不在费用日历中增加环比、类别占比或趋势图。

**月历日期格**

- 使用七列月历，显示星期标题和当前月份全部日期；
- 每个有记录的日期显示日期数字、当天费用合计和记录数量；没有记录的日期不显示无意义的零金额；
- 今天和当前选中日期均有明确且可区分的状态；周末可以适度弱化，但不能影响阅读；
- 金额或记录数较大时不得撑破日期格、遮挡内容或与相邻日期重叠；
- 金额和记录数量不能只通过颜色表达；
- 日期按用户本地时间归属，不能因 UTC 时区转换造成记录日期错位；
- 日期格支持鼠标和键盘操作，并提供可被辅助技术识别的完整日期、记录数和金额说明。

**日期选择**

- 当前月份默认选中今天；查看其他月份时默认不选中日期，并提示用户选择一天；
- 点击任意日期后显示当天明细；点击无记录日期时也显示明确空状态和“为这天记一笔”；
- 切换车辆时，如选中日期仍属于当前月份则保留该日期；
- 新增、编辑或删除记录后继续保留当前月份和选中日期。

**当天摘要和记录明细**

- 显示完整日期和星期、当天费用合计、当天记录数量及“为这天记一笔”；
- 当天记录默认按发生时间从早到晚排列；
- 每条记录显示发生时间、费用类别及图标、金额，以及已填写的商家或地点；
- “全部车辆”模式必须显示每笔记录所属车辆，具体车辆模式可以弱化重复的车辆名称；
- 加油或充电记录可补充显示补能量和当前里程；
- 同一天记录较多时允许明细区域向下扩展或在区域内滚动，不能截断记录，第一版不增加分页。

**查看、编辑和删除记录**

- 点击当天记录后，在右侧抽屉查看完整记录详情，并提供编辑和删除操作；
- 编辑成功后立即重新计算相关日期和月份数据；修改日期后记录从原日期移动到新日期，修改车辆后按当前车辆条件决定是否继续显示；
- 删除记录前必须二次确认；删除后立即更新当天金额、记录数、有费用天数和当月合计；
- 关闭详情或取消编辑后返回原日历上下文；新增、编辑和删除均无需刷新页面，并提供明确的成功或失败反馈。

**从日历新增记录**

- 页面顶部提供“为今天记一笔”，选中日期区域提供“为这天记一笔”；
- 自动带入目标日期；当前为具体车辆时带入该车辆，当前为“全部车辆”时带入默认车辆，没有默认车辆时要求选择车辆；
- 不预设费用类别和金额；发生时间默认使用当前时间，但日期必须保持为目标日期；
- 保存后返回费用日历时恢复原月份，并选中记录的发生日期；新记录立即计入对应日期和当月摘要。

**空状态**

- 无车辆时不显示不可操作的空日历，提示先添加车辆并提供“新增第一辆车”；
- 有车辆但完全无记录时显示当前月份空日历，提示尚无用车费用记录并提供“记录第一笔费用”；
- 有历史记录但当前月份无记录时正常显示日期和零值摘要，并提供“为今天记一笔”或“为这天记一笔”；
- 选中日期没有记录时明确显示“这一天暂无费用记录”，不显示空白记录列表。

**响应式与操作要求**

- 宽屏使用月历与当天明细双栏，中等宽度可调整比例，窄屏改为上下布局；
- 页面不得产生整体横向滚动，星期标题与日期列必须始终对齐；
- 日期、记录及所有操作均可使用键盘完成；今天、选中、空状态和操作结果不能只依赖颜色表达；
- 打开详情抽屉后焦点进入抽屉，关闭后返回触发该抽屉的记录。

### 5.4 数据分析【V1.9 修改】

数据分析用于回答选定时间内“花了多少钱、费用发生在哪些时间和类别、相比上一周期如何变化、不同车辆如何分布，以及哪些记录构成当前结果”。本模块只分析费用结构和趋势，不重复能耗统计中的油耗、电耗和补能区间分析。

**页面结构**

1. 页面标题和当前分析条件摘要；
2. 时间、车辆和类别筛选；
3. 核心指标；
4. 费用趋势；
5. 月份费用对比；
6. 费用类别构成；
7. 全部车辆模式下的车辆费用对比；
8. 数据不足或空状态说明。

**时间、车辆和类别条件**

- 时间范围提供本月、近三个月、今年、全部时间和自定义；本月为当月 1 日至今天，近三个月为本月及之前两个自然月截至今天，今年为当年 1 月 1 日至今天；
- 全部时间使用当前车辆和类别条件下最早记录至最新记录；自定义范围必须同时填写开始和结束日期，开始日期不得晚于结束日期，条件无效时停止计算并在日期附近说明原因；
- 车辆条件与顶部全局车辆选择器同步，支持具体车辆和全部车辆；切换后全部指标、趋势和构成统一重新计算，不跨车辆混算里程；
- 类别支持全部类别或单个费用类别；选择后页面明确展示当前类别，全部结果使用同一类别条件；
- 从分析进入详细记录再返回时保留时间、车辆和类别条件；“清除筛选”恢复“今年、全部类别”，车辆继续服从顶部全局车辆条件。

**核心指标和计算规则**

- 显示总费用、记录数量、平均每月费用、较上一周期变化和最高费用类别；
- 具体车辆模式显示每公里综合成本；全部车辆模式以有费用车辆数替代，不展示跨车辆每公里成本；
- 平均每月费用以筛选范围覆盖的自然月数量为分母，无记录月份也计入，避免只按活跃月份计算导致结果虚高；
- 上一周期使用与当前范围等长且紧邻当前范围之前的日期区间；上一周期没有费用时显示“暂无可比数据”，不显示错误百分比；
- 每公里成本使用当前范围内至少两条含有效里程且严格递增的记录计算；条件不足时显示具体原因，不显示误导性的零值。

**费用趋势**

- 时间范围不超过 31 天时按日展示，超过 31 天时按月展示；范围内没有记录的日期或月份补零，保证时间轴连续；
- 每个趋势点展示日期或月份、费用金额和记录数；点击后进入详细记录并带入对应日期范围、车辆和类别；
- 图表下方保留可读的数据列表，不能只依赖图形或颜色表达；当前条件无数据时显示空状态，不绘制空图表。

**月份费用对比**

- 按时间顺序展示月份、费用总额、记录数量、相比上月的金额变化和百分比变化；
- 首个月份或上月费用为零时显示“暂无可比数据”；点击月份进入带有当前车辆和类别条件的详细记录。

**费用类别构成**

- 默认按金额从高到低排列，每个类别显示名称、金额、记录数和总费用占比；
- 各类别金额之和必须等于当前总费用；点击类别进入详细记录并保留当前时间和车辆条件；
- 已选择单一类别时展示该类别汇总，不生成没有意义的多类别构成图。

**车辆费用对比**

- 仅在“全部车辆”模式显示，每辆车展示费用金额、记录数和总费用占比，默认按金额从高到低排列，无费用车辆排在有费用车辆之后；
- 点击车辆进入对应详细记录；不跨车辆计算里程、每公里成本、油耗或电耗。

**空状态和数据联动**

- 无车辆时提示先新增车辆；完全无记录时提示记录第一笔费用；当前筛选无结果时显示当前条件并提供清除筛选和记账入口；
- 每公里成本等局部数据不足时保留其他有效分析结果，只说明该指标无法计算的原因；
- 新增、编辑、删除或导入记录后，当前条件下的指标、趋势和构成立即更新，无需刷新页面。

**手机端、响应式与可访问性**

- 手机端使用单列布局，筛选项和指标卡自动换行；页面不得产生整体横向滚动，图表、月份、类别和车辆条目不得被裁切；
- 主要按钮和可点击数据项提供适合触控的操作区域，手机横屏和竖屏均可正常使用；
- 所有筛选和下钻均可通过键盘完成；图表提供文字数据，选中、增长、下降和错误状态不能只依赖颜色区分。

**正式视觉布局【V1.9 新增】**

- 手机端自上而下依次为页面标题、车辆选择器、紧凑筛选区、核心指标带、费用趋势、费用类别构成、月份及车辆对比和“查看详细记录”；底部导航保持可见并选中“数据分析”；
- 本月、近三个月、今年和全部时间优先使用分段控件；自定义时间、车辆和类别可使用选择器或展开式筛选区。筛选区必须显示已生效条件，不能因视觉简化丢失清除筛选和自定义范围；
- 首屏核心指标带优先展示总费用、记录数量和平均每月费用三个指标；较上一周期、最高费用类别、每公里成本或有费用车辆数继续在下方展示，不得删除；
- 费用趋势是第一主图，使用品牌青绿折线和低对比度面积填充；当前点通过外圈、标签或形状突出，不能仅靠颜色；单位“元”与时间粒度必须明确；
- 费用类别构成是第二主图，可采用圆环图与按金额降序的文字列表组合；列表必须显示类别名称、金额、记录数和占比，各项可下钻；单类别或无数据时执行既有降级规则；
- 月份费用对比和车辆费用对比可以折叠到主图之后，但必须有明确标题和入口；桌面端可与类别构成并列，手机端不得通过横向拖动整页查看；
- “查看详细记录”作为分析结果的明确下钻操作，必须带入当前时间、车辆和类别条件；返回后恢复原筛选和滚动位置；
- 图表使用真实可访问的图形或 HTML/SVG 实现，不得把视觉稿中的图表裁切成图片；每张图都需有等价数据列表、加载状态、空状态和错误状态。

### 5.5 详细记录【V1.7 修改】

详细记录是全部费用数据的统一明细入口，用于快速找到目标记录、查看完整内容、安全完成记录管理，并承接首页、费用日历和数据分析的条件下钻。页面必须同时适合电脑和手机浏览器使用。

**模块目标**

- 支持通过车辆、关键词、类别、日期和金额等条件快速定位记录；
- 清楚展示当前筛选结果的费用规模和时间范围；
- 完成查看、编辑、删除和复制记录的操作闭环；
- 在刷新、浏览器前进或后退以及跨模块跳转后保持可理解的查询上下文；
- 手机端使用适合单手浏览和触控的布局，不依赖横向滚动查看主要内容。

**页面结构**

1. 页面标题和当前车辆；
2. 筛选、搜索和排序区域；
3. 已生效条件及结果汇总；
4. 记录列表；
5. 加载更多区域；
6. 记录详情、编辑或复制抽屉。

手机端以上区域按纵向排列；电脑端可在空间允许时并排展示部分筛选条件。

**筛选、搜索和条件校验**

- 支持按车辆、关键词、费用类别、开始日期、结束日期、最低金额和最高金额组合筛选；
- 关键词可匹配类别名称、车辆名称、商家或地点及备注；
- 页面内明确显示当前车辆条件，并与顶部全局车辆选择器保持同步；
- 从首页、费用日历或数据分析进入时，优先应用入口传入的车辆、类别和日期等条件；
- 开始日期不得晚于结束日期，最低金额不得高于最高金额，金额条件必须为大于或等于零的有效数字；
- 条件无效时在对应区域显示明确提示，不执行错误筛选，提示不能只依赖颜色表达；
- 已生效条件以清晰的条件项展示，每项可单独移除，并提供“清除全部”；
- 清除全部时清除关键词、类别、日期和金额条件，排序恢复为日期从早到晚，车辆继续与顶部全局车辆条件保持一致。

**URL 条件保持**

- 车辆、关键词、类别、日期范围、金额范围和排序方式写入 URL 查询参数；
- 刷新页面、浏览器前进或后退后，筛选状态和结果保持一致；
- 从其他模块下钻时，详细记录能够完整还原入口条件；
- URL 中不存在的车辆、类别或其他无效参数应被安全忽略或回退，并给出非阻塞提示，页面不能报错；
- 用户可通过复制当前地址重新打开同一组查询结果。

**结果汇总**

- 根据全部筛选结果展示记录数量、总金额、平均单笔金额和结果覆盖的日期范围；
- 所有汇总指标必须使用同一组筛选条件计算；
- 无结果时总金额显示为 `¥0.00`，平均单笔金额和日期范围显示“—”，不能出现 `NaN`、`Infinity` 或其他异常数值；
- 汇总基于全部筛选结果，不受当前已经加载并显示的记录条数影响。

**记录列表和排序**

- 电脑端使用语义清晰的表格，手机端切换为单列记录卡片；
- 每条记录优先展示日期和时间、类别名称及图标、金额、车辆名称、商家或地点、里程和备注摘要；
- 加油或充电记录可补充展示加油量、充电量、单价或加满、充满状态等关键数据；
- 无内容的非必要字段不占用空白区域；金额作为主要视觉信息突出，日期、车辆和辅助字段降低视觉层级；
- 全部车辆模式必须清楚显示所属车辆；具体车辆模式可以弱化车辆名称；
- 默认按发生日期从早到晚排列，支持日期从晚到早、金额从低到高和金额从高到低；
- 主排序字段相同时，使用记录时间和记录编号作为稳定的次级排序条件，避免顺序跳动；
- 从“保存并查看记录”或复制保存进入时，目标记录自动进入可视区域并短暂高亮，高亮不能阻碍后续操作。

**记录详情**

- 点击记录主体打开详情抽屉，不直接进入编辑状态；
- 详情显示完整日期和时间、车辆、类别、金额、里程、商家或地点、完整备注、创建时间和最后修改时间；
- 加油或充电记录还需显示能源类型、数量、单价、油品标号或充电方式、加满或充满状态等适用字段；
- 详情抽屉提供编辑、复制、删除和关闭操作；
- 打开抽屉后键盘焦点进入抽屉，关闭后返回原记录，桌面键盘和手机触控均可操作。

**编辑记录**

- 可从详情或记录操作菜单进入编辑，沿用用车记账的字段和校验规则；
- 保存成功后立即刷新记录列表和汇总，并保留原筛选、排序、已加载数量和可继续操作的列表位置；
- 修改后的记录不再符合当前筛选条件时，从结果中移除并显示保存成功提示；
- 保存失败时保留用户已填写内容，显示失败原因并允许重试；
- 取消编辑不得修改原记录。

**删除记录**

- 删除前必须二次确认，并在确认信息中显示记录日期、车辆、类别和金额；
- 取消删除后所有数据和页面上下文保持不变；
- 删除成功后立即更新记录列表、汇总以及首页、费用日历、数据分析和适用的能耗统计；
- 删除后尽量保持用户原来的列表位置；
- 删除失败时原记录必须保留，并显示可理解的错误信息；
- 点击记录主体只能查看详情，不得直接触发删除。

**复制为新记录**

- 复制操作先打开预填的新记录表单，不得在用户点击后立即写入数据库；
- 复制原记录的车辆、类别及业务字段，生成新的记录编号，并将发生日期和时间默认更新为当前时间；
- 用户可在保存前修改金额、里程、补能数据、商家或地点及备注；
- 用户确认保存后才创建新记录，取消复制不产生任何数据；
- 保存成功后返回详细记录，定位并短暂高亮新记录；
- 复制表单使用与新增记录相同的车型适配、字段清理、校验、重复提交保护和失败保护规则。

**大量记录和加载更多**

- 首次最多展示 30 条记录，每次继续加载 30 条；
- 页面显示“已展示 X / 共 Y 条”；全部展示后隐藏“加载更多”操作；
- 筛选条件或排序方式变化时，恢复为首次展示数量；
- 加载更多只改变可见记录数量，不能改变筛选结果汇总；
- 第一版不使用无限滚动和虚拟列表。

**空状态和异常状态**

- 无车辆时说明需要先创建车辆，并提供新增车辆入口；
- 有车辆但完全无记录时提供“记录第一笔费用”入口；
- 存在记录但当前筛选无结果时显示当前条件，并提供调整条件、清除筛选和记账入口；
- 数据加载中显示稳定的加载状态，加载失败时显示错误原因和重试入口；
- 编辑、删除或复制失败时保留现有数据和用户上下文，不得误报成功。

**手机端、响应式与可访问性**

- 手机端使用记录卡片，不展示被压缩的桌面表格，页面主体不得产生横向滚动；
- 主要按钮和记录操作提供约 44×44 像素或以上的触控区域；
- 编辑、复制和删除集中在明确的操作区域或菜单中，降低误触风险；
- 筛选条件较多时允许折叠，但当前生效条件和结果数量必须容易找到；
- 输入框、按钮和图标均提供可识别名称，焦点状态清晰；
- 支持常见窄屏、手机横屏和竖屏；
- 金额、错误、选中、高亮和禁用等状态不能只依赖颜色区分。

**跨模块联动**

- 首页统计卡片、月份趋势、类别构成，费用日历日期或月份，以及数据分析的月份、趋势、类别和车辆均可带条件进入详细记录；
- 入口条件必须准确映射，详细记录的数量和总金额与来源模块保持一致；
- 从详细记录返回后，来源页面的筛选条件应保持；
- 新增、编辑、删除、复制或导入数据后，详细记录和其他关联模块再次显示时必须使用最新数据。

### 5.6 能耗统计【V1.2 修改】

能耗统计用于回答“实际耗多少、每公里花多少钱、变化趋势如何、结果根据哪些记录计算”。所有结果只针对一辆具体车辆，不把不同车辆的数据混合计算。

**页面结构**

1. 页面标题、当前车辆、能源类型和“记录加油或充电”快捷入口；
2. 时间范围筛选；
3. 核心指标卡片；
4. 能耗趋势；
5. 能源价格与成本趋势；
6. 数据质量与计算说明；
7. 区间计算明细；
8. 补能记录管理。

**车辆和时间条件**

- 能耗统计必须选择一辆具体车辆，并支持在页面内切换车辆；页面内选择结果与顶部全局车辆选择器同步；
- 当前为“全部车辆”时不计算混合结果，应显示车辆选择入口；
- 时间范围提供近三个月、近六个月、近十二个月、全部和自定义范围，默认近十二个月；
- 切换车辆或时间范围后，全部指标、趋势和明细立即重新计算；
- 时间范围之外的上一条有效记录可以作为首个区间的计算起点，并明确标记为“计算基准”。

**核心指标和分车型显示**

- 燃油车：平均油耗、最近完整区间油耗、相对上一区间变化、每公里燃油成本、平均油价、统计里程、加油金额和加油量；
- 纯电动车：平均电耗、最近完整区间电耗、相对上一区间变化、每公里充电成本、平均电价、统计里程、充电金额和充电量；
- 插电式混动车：分别显示平均及最近油耗、平均及最近电耗、每公里燃油成本、每公里充电成本、综合能源成本，以及燃油费用和充电费用占比；
- 油耗和电耗使用各自单位展示，不能直接相加或生成没有科学依据的综合能耗值；
- 数据条件不满足时显示“数据不足”及具体原因，不能显示误导性的 0。

**能耗趋势**

- 使用折线图按日期显示每个完整区间的油耗或电耗，并显示历史加权平均参考线；
- 数据点展示区间起止日期、起止里程、行驶距离、补能量、区间能耗和区间能源成本；
- 点击数据点后展开对应区间的计算明细；
- 插电式混动车的油耗和电耗分别使用独立图表，不共用同一纵轴；
- 没有足够数据时不显示空图表，应显示补充数据的具体指引。

**能源价格与成本**

- 显示每次加油单价或充电单价的变化趋势；
- 单价未填写但金额和数量完整时，使用“金额 ÷ 数量”计算单价；
- 显示每个完整区间的能源成本/公里；
- 显示统计期内能源总费用、平均单价和总补能量；
- 插电式混动车分别显示燃油费用、充电费用及两者占比。

**完整区间和计算规则**

- 燃油车以“加满到下一次加满”形成完整区间；纯电动车以“充满到下一次充满”形成完整区间；
- 两个完整边界之间的部分加油或部分充电，其数量和费用累计计入该区间，但不能单独作为完整区间终点；
- 区间距离 = 结束里程 − 开始里程；
- 区间油耗 = 区间累计加油量 ÷ 区间距离 × 100；
- 区间电耗 = 区间累计充电量 ÷ 区间距离 × 100；
- 区间能源成本 = 区间累计补能金额 ÷ 区间距离；
- 平均能耗按“所有有效区间的总补能量 ÷ 总有效里程 × 100”加权计算，不能直接平均各区间数值；
- 插电式混动车的综合能源成本按有效统计范围内燃油和充电总费用除以有效里程计算；至少需要两个有效且递增的里程点；
- 缺少里程、缺少补能量、结束里程不大于开始里程、被用户排除或尚未形成完整边界的记录，不生成有效区间。

**区间计算明细**

- 按最近区间优先显示区间日期、起止里程、行驶距离、补能量、补能费用、百公里能耗、每公里能源成本和状态；
- 每个区间提供“查看计算依据”，展开该区间包含的全部原始补能记录；
- 每个统计结果必须能够追溯到参与计算的原始记录。

**补能记录管理**

- 显示日期、里程、补能类型、数量、单价、金额、是否加满或充满、是否参与计算和编辑操作；
- 支持按全部、有效、数据不完整和已排除筛选；
- 支持查看、编辑、标记“不参与能耗计算”和恢复参与计算；
- 支持快速新增当前车辆的加油或充电记录；
- 排除、恢复或编辑记录后，所有指标、趋势和区间明细立即更新。

**数据质量提示**

- 显示有效完整区间、缺少里程、缺少补能量、未标记加满或充满、里程重复或倒退、已排除记录的数量；
- 每种问题提供明确处理入口，例如补充里程、编辑记录或恢复计算；
- 分别处理无车辆、选择全部车辆、无补能记录、只有一条记录、没有完整区间、全部记录被排除和数据异常等状态。

**本模块第一版暂时不做**

- 不连接车辆、OBD、加油平台或充电平台；
- 不读取油量、电池剩余电量、天气、路况或驾驶行为；
- 不预测未来能耗，不与同车型或其他用户比较；
- 不把油耗和电耗强行换算成一个综合能耗值。
### 5.7 车辆管理【V1.9 修改】

车辆管理是车辆资料和车辆关联数据的统一管理入口，用于查看当前管理的车辆、维护车辆基础资料、设置默认车辆，并安全处理会影响历史记录和统计结果的车辆变更。页面必须同时适合电脑和手机浏览器使用。

**模块目标**

- 让用户快速识别每辆车的基础资料、当前里程、累计费用和记录情况；
- 完成新增、编辑、设为默认和删除车辆的操作闭环；
- 对初始里程、能源类型和级联删除等数据敏感操作提供明确保护；
- 提供记账和查看车辆记录的快捷入口，并与其他模块保持实时一致；
- 手机端采用适合单手浏览和触控的布局，不依赖横向滚动完成主要操作。

**本次数据范围**

本次继续使用现有车辆字段，不新增数据库字段：

- 唯一编号；
- 车辆名称；
- 能源类型：燃油、纯电、插电式混动；
- 初始里程；
- 车牌号码，可选；
- 是否为默认车辆。

当前里程、累计费用、记录数量和最近记录日期均由现有车辆及记录实时计算，不重复存储。品牌型号、VIN、购车日期、购车价格、油箱或电池容量、保险和保养资料暂不纳入本次范围。

**页面结构**

1. 页面标题、车辆数量和主要操作；
2. 新增车辆入口和“备份与恢复”次要入口；
3. 默认车辆置顶的车辆卡片列表；
4. 新增或编辑车辆的抽屉或对话框；
5. 删除车辆的风险确认弹窗；
6. 无车辆、无记录、加载失败和操作失败等状态。

桌面端可以使用多列车辆卡片；手机端使用单列卡片。页面主体不得产生横向滚动。

**车辆卡片和计算规则**

每张车辆卡片显示：

- 车辆名称；
- 默认车辆标识；
- 已填写的车牌号码；
- 能源类型；
- 当前里程；
- 累计费用；
- 关联记录数量；
- 最近一次记录日期，无记录时显示“暂无记录”。

当前里程按以下顺序确定：

1. 存在有效里程记录时，取该车辆全部记录中的最高有效里程；
2. 没有有效里程记录时，使用车辆初始里程；
3. 缺失、非数字或其他异常里程不得参与计算，也不得导致页面崩溃。

默认车辆固定排在列表首位；其他车辆保持稳定顺序，不能因重新渲染无故跳动。空的非必要字段不占用卡片空间。

**车辆卡片操作**

- “记一笔”进入用车记账并自动选中对应车辆，保存前仍允许用户切换车辆；
- “查看记录”进入详细记录并自动应用对应车辆筛选，页面清楚显示当前筛选车辆；
- “编辑车辆”打开已有资料；
- 非默认车辆提供“设为默认”；
- 提供“删除车辆”，但必须与普通操作分隔并使用明确的危险操作样式。

手机端优先展示“记一笔”和“查看记录”，编辑、设为默认和删除可以收拢至“更多操作”菜单，避免卡片堆叠过多按钮。

**新增车辆**

- 桌面端使用侧边抽屉或对话框，手机端使用接近全屏的表单界面；
- 车辆名称必填，去除首尾空格后为 1～30 个字符；
- 能源类型必填，只能选择燃油、纯电或插电式混动；
- 初始里程必填，必须是不小于 0 的有效数字，最多保留 1 位小数；
- 车牌号码选填，去除首尾空格后最多 20 个字符，不强制限定中国车牌格式；
- 第一辆车自动设为默认车辆；已有车辆时允许用户选择将新车设为默认车辆；
- 车辆名称或非空车牌与已有车辆重复时给出非阻塞提醒，但允许用户确认后继续保存；
- 保存过程中禁用重复提交；成功后关闭表单、刷新列表并显示结果；失败时保留全部输入并提供可理解的错误信息。

**编辑车辆与数据敏感字段**

- 修改车辆名称或车牌号码后，车辆管理、详细记录、费用日历、数据分析、能耗统计和车辆选择器立即使用最新信息；
- 没有关联记录时，可以按新增规则修改初始里程和能源类型；
- 已有关联记录时，新的初始里程不得高于该车辆历史记录中的最低有效里程；校验失败时不得保存任何字段；
- 修改初始里程可能影响当前里程或统计展示时，保存前必须说明影响；
- 已有关联补能记录时修改能源类型必须二次确认；
- 修改能源类型不得删除、转换、覆盖或静默重新解释历史记录；历史记录继续按自身已保存的数据展示，新的能源类型仅影响后续新增记录的车型适配和统计适用规则；
- 实现前必须验证现有数据结构能够保持上述历史兼容性；如无法保证，应先提出数据兼容和迁移方案，不得直接修改。

**未保存内容保护**

- 表单内容发生变化后，关闭、返回或离开页面时提示当前修改尚未保存；
- 用户可以继续编辑或放弃修改；
- 表单没有变化时直接关闭；
- 保存成功后不再触发离开提醒；
- 打开表单后焦点进入第一个可编辑字段，关闭后返回原触发按钮。

**默认车辆规则**

- 存在车辆时必须有且只有一辆默认车辆；
- 第一辆车自动成为默认车辆；
- 设置新默认车辆后，原默认状态自动取消；
- 当前已是默认车辆时不显示“设为默认”；
- 设置默认车辆属于可恢复的低风险操作，不要求额外确认，但必须显示成功或失败反馈；
- 默认车辆用于下次打开 App 以及在“全部车辆”状态下新增记录时的默认选择；
- 设置默认车辆不得强制中断用户当前正在查看或编辑的其他车辆上下文。

**删除车辆安全**

- 删除确认必须显示车辆名称、关联记录数量、关联费用总额，以及首页、费用日历、数据分析、详细记录和能耗统计将受到影响的说明；
- 无关联记录时进行一次明确确认；存在关联记录时必须加强确认，明确说明车辆及全部关联记录将永久删除且无法撤销；
- 取消删除时车辆、记录、默认状态和页面上下文全部保持不变；
- 车辆与其关联记录必须作为一个完整操作处理；失败时不得出现只删除车辆或只删除部分记录的状态；
- 删除默认车辆后，如仍有其他车辆，系统自动选择其中一辆作为新的默认车辆；
- 删除最后一辆车后进入无车辆状态，并安全清除指向该车辆的当前选择或筛选；
- 删除成功后所有关联模块立即重新计算；失败时保留原数据并显示可理解的错误信息。

**空状态和异常状态**

- 无车辆时显示添加第一辆车的说明和主操作，不显示无意义的空卡片或零值统计；
- 有车辆但无记录时，当前里程使用初始里程，累计费用显示 `¥0.00`、记录数显示 `0`，并提供“记第一笔”；
- 数据加载中显示稳定的加载状态；加载失败时不显示误导性空列表，提供错误说明和重试入口；
- 新增、编辑、设为默认或删除失败时不得误报成功，且不得破坏原有数据。

**手机端、响应式与可访问性**

- 手机端使用单列车辆卡片，长车名和长车牌应换行或合理截断，不能破坏布局；
- 主要按钮和可点击操作提供约 44×44 像素或以上的触控区域；
- 表单在软键盘弹出后，当前输入框、错误提示和保存按钮仍可访问；
- 页面和底部操作适配手机安全区域，不能被系统手势区域遮挡；
- 所有图标按钮提供可识别名称，默认、危险、错误和禁用状态不能只依赖颜色区分；
- 确认弹窗和表单打开后管理键盘焦点，关闭后恢复到触发操作；
- 保存成功、设置默认、删除结果和字段错误能够被辅助技术感知。

**正式视觉布局【V1.9 新增】**

- 手机端页面顶部显示“车辆管理”、车辆数量、“新增车辆”主按钮和视觉层级更低的“备份与恢复”入口；底部导航保持可见并选中“更多”；
- 默认车辆卡片置顶，卡片头部展示车辆名称、默认标识、能源类型和已填写的车牌；默认标识不能只用颜色表达；
- 卡片中部可使用低对比度车辆剪影作为装饰，但车辆资料和操作必须为真实组件；无装饰资源、资源加载失败或小屏隐藏装饰时，卡片布局和功能不得受损；
- 卡片指标优先展示当前里程、累计费用和记录数，最近记录日期作为辅助信息；数值较长或文字放大时允许分行，不得溢出或缩小到不可读；
- “记一笔”和“查看记录”作为卡片常驻操作；编辑、设为默认和删除收拢至“更多操作”，菜单必须有可访问名称、键盘焦点和关闭后的焦点恢复；
- 删除操作与普通操作保持视觉和空间分隔，不能把危险操作设置为默认焦点，也不能因收拢菜单降低既有二次确认标准；
- 多辆车按稳定顺序纵向排列，并在首张卡片后露出下一张卡片的标题或完整卡片，帮助用户理解列表可继续滚动；不得制造虚假的轮播或分页；
- 无车辆时以“添加第一辆车”为唯一主操作；加载失败时显示重试；有车辆无记录时保留车辆资料、初始里程和记账入口。

**数据兼容和上线约束**

- 本次不新增车辆字段和数据库表，原则上不触发数据库版本迁移或备份格式变化；
- 已有车辆、记录、默认状态和现有 JSON 备份必须继续兼容；
- 不新增系统权限、网络接口、第三方服务或第三方组件依赖；
- 删除关联数据、修改能源类型和修改初始里程属于重点回归范围；
- 如开发过程中发现必须改变数据结构，应先完成现有数据、备份恢复、回滚、TestFlight 和 App Store Release Impact Check，再决定是否实施。

### 5.8 更多【V1.10 新增】

**模块目标**

- “更多”是手机端底部导航未直接展示功能的集中入口，用于访问车辆管理、能耗统计和数据管理；
- 页面必须帮助用户理解每个入口的用途、可用状态及必要的真实数据摘要，不得退化为普通文字链接集合；
- 本次只调整页面信息架构、视觉与导航交互，不改变车辆、费用记录、能耗计算、备份恢复或全局车辆条件的数据规则。

**页面顶部和信息分组**

- 手机端页面顶部显示一级标题“更多”和副标题“管理车辆、能耗与本地数据”；
- 手机端不显示全局车辆选择器和单独“+”按钮；用车记账继续通过首页主按钮、车辆卡片、费用日历和详细记录等既有入口进入；
- 页面包含“车辆与用车”和“数据与安全”两个区块；
- “车辆与用车”包含车辆管理和能耗统计，“数据与安全”包含数据管理；
- 区块标题用于建立阅读层级，不应做成抢占空间的大型卡片；页面不得因内容较少制造异常固定高度或大面积不可解释留白。

**入口卡片通用结构**

- 每个功能使用独立卡片，包含左侧统一线性图标、功能名称、一句话说明、可选状态摘要和右侧方向箭头；
- 卡片整体作为一个明确的导航目标，不在同一卡片内设置语义重复的第二个跳转按钮；
- 右侧箭头仅作方向提示并从辅助技术隐藏；卡片的可访问名称应包含功能名称和必要状态；
- 卡片需实现默认、悬停、按下、键盘焦点和不可用或数据不足状态，状态不得只依赖颜色；
- 卡片使用 V1.9 的页面背景、颜色、字体、间距、圆角、阴影和图标规范，不得显示浏览器默认紫色链接样式，不得使用 Emoji 或字体字符冒充产品图标。

**车辆管理入口**

- 标题为“车辆管理”，说明为“管理车辆资料、默认车辆和关联记录”；
- 显示真实车辆数量，例如“共 2 辆”；存在默认车辆时可补充“默认：城市通勤车”；
- 无车辆时显示“还没有车辆”和“添加第一辆车”的明确行动提示；
- 点击卡片进入车辆管理页面；返回后回到“更多”并保持底部导航当前状态。

**能耗统计入口**

- 标题为“能耗统计”，说明为“查看油耗、电耗和能源成本”；
- 当前选择具体车辆时，状态摘要显示该车辆名称及适用能耗类型，例如“查看城市通勤车的油耗数据”；
- 当前为“全部车辆”时显示“进入后选择一辆车”，点击后进入能耗统计并由该页面要求选择具体车辆；
- 无车辆时显示“添加车辆后可使用”，不得用虚构的零油耗、零电耗或空图表示错误状态；点击后应引导至新增车辆或进入能耗页面的明确空状态；
- 不允许跨车辆、跨能源类型在该卡片上混算能耗数值。

**数据管理入口**

- 标题为“数据管理”，说明为“导出备份、导入与恢复本地数据”；
- 显示“仅保存在本机”状态标签，准确传达当前本地数据边界；
- 点击后直接进入现有备份与恢复区域，不增加多余中转页；
- 必须保留备份校验、合并或覆盖选择、覆盖确认、失败保护和数据一致性规则；
- 不显示云同步、账号或远程存储暗示，不上传本地车辆或记录数据。

**导航与返回**

- 手机端底部导航保持可见并选中“更多”；选中状态同时使用底色或形状、字重和颜色表达；
- 车辆管理、能耗统计和数据管理均可从“更多”到达，不存在仅桌面可访问的管理功能；
- 浏览器后退或页面内返回操作应回到“更多”，不得丢失底部导航状态或形成循环跳转；
- 桌面端不新增“更多”一级导航；直接访问“更多”地址时仍需正常显示，并保留桌面侧边导航和顶部全局栏。

**响应式布局**

- ≤767px 使用单列入口卡片；页面左右留白默认 20px，320px 宽度可降为 16px；
- 390px 宽手机首屏应完整显示标题，并至少显示两个完整入口卡片；
- 768～1023px 可将车辆管理和能耗统计组成双列，数据管理横跨整行；
- ≥1024px 直接访问时可使用合理的双列或三列布局，但必须限制内容最大宽度，不能把手机卡片等比放大成超宽内容；
- 320px、375px、390px、414px、430px、768px、1024px和1280px宽度下不得出现整体横向滚动、遮挡或卡片文字重叠；
- 长车辆名称允许换行或合理省略，但不得挤压图标、状态和方向箭头；200%文字缩放时允许卡片增高。

**空状态、异常和可访问性**

- 无车辆、当前为全部车辆、数据加载中和数据读取失败应分别显示准确说明及可执行下一步；
- 单个入口状态读取失败不能导致整个“更多”页崩溃，不能用空白卡片或错误的 0 冒充失败；
- 所有核心入口在断网状态下仍可打开对应本地功能；
- 每张卡片及底部导航触控目标至少约 44×44px，普通文字对比度至少达到 4.5:1；
- 页面支持键盘访问、清晰焦点轮廓、合理阅读顺序、安全区域和200%文字缩放；
- 状态摘要、不可用原因和当前导航状态需要有文字、图标、边框或形状辅助表达，不能只依赖颜色。

**数据兼容和上线约束**

- 本次不新增数据库字段、表或 schema 版本，不改变 JSON 备份格式；
- 已有车辆、记录、当前车辆、默认车辆和备份数据必须继续兼容；
- 不新增账号、网络接口、云同步、远程字体、远程图片、系统权限、追踪 SDK、Signing 或 Entitlements；
- Debug、Production Build 和离线模式均需验证；后续迁移到 iOS 时使用相同信息架构，但需重新验证安全区域、VoiceOver 和原生返回行为。

## 6. 首页和各模块之间的关系

### 6.1 全局车辆条件

- 顶部选择的当前车辆是首页、费用日历、数据分析和详细记录的默认车辆条件；
- 在上述页面切换车辆后，其他页面再次打开时应保持一致；
- 选择“全部车辆”后，首页和数据分析可展示跨车辆汇总；
- 能耗统计必须落到具体车辆，不能汇总不同能源类型车辆的能耗数值。

### 6.2 新增记录后的联动

记录保存成功后，相关页面数据必须立即更新，包括：

- 首页费用、次数、最近记录和摘要；
- 费用日历对应日期及当月合计；
- 数据分析趋势、类别占比和车辆对比；
- 详细记录列表及筛选合计；
- 符合计算条件时的能耗统计。

### 6.3 从首页进入模块【V1.3 修改】

| 首页内容 | 点击后的目标 |
| --- | --- |
| 本月费用或记录数 | 详细记录，自动筛选当前车辆和本月 |
| 某个费用类别 | 详细记录，自动筛选当前车辆、当前月份和该类别 |
| 最近记录 | 打开该记录的详情抽屉 |
| 费用趋势中的月份 | 详细记录，自动筛选对应月份 |
| 能耗摘要 | 当前车辆的能耗统计 |
| “记一笔” | 用车记账，并默认带入当前车辆 |
| “记录加油”或“记录充电” | 用车记账，并默认带入当前车辆和补能类别 |
| “查看全部记录” | 详细记录，并保留当前车辆条件 |
| 全部车辆费用对比中的车辆 | 将该车辆设为当前车辆并切换至具体车辆首页 |
| 数据提醒 | 对应记录编辑、补能记录或能耗统计处理入口 |
### 6.4 编辑和删除后的联动

- 修改金额、日期、车辆、类别、里程、加油量或充电量后，所有相关统计必须重新计算；
- 删除记录后，该记录必须从首页、日历、分析、详细记录和能耗统计中同时消失；
- 页面应保留用户可继续操作的上下文，不能要求刷新浏览器才能看到结果。

### 6.5 进入和完成用车记账【V1.4 新增】

| 来源或操作 | 用车记账中的结果 |
| --- | --- |
| 顶部“记一笔” | 自动带入当前具体车辆；当前为全部车辆时带入默认车辆 |
| 首页“记录加油”或“记录充电” | 自动带入当前车辆和对应补能场景，并校验车型是否适用 |
| 费用日历“为这天记一笔” | 自动带入所选日期和当前车辆 |
| 详细记录“复制” | 以原记录为基础创建新编号的记录，并使用新的发生时间 |
| 保存普通费用 | 首页、日历、分析和详细记录立即更新 |
| 保存补能记录 | 首页、日历、分析、详细记录和能耗统计立即更新 |
| 保存并查看记录 | 进入详细记录，并让刚保存的记录容易识别 |
| 保存并再记一笔 | 保留车辆和类别，清空本次业务数据并更新时间 |
| 编辑并保存 | 返回原查看或列表上下文，所有关联模块使用最新数据 |

### 6.6 车辆管理联动【V1.8 新增】

| 车辆管理操作 | 关联模块结果 |
| --- | --- |
| 点击“记一笔” | 进入用车记账并自动带入该车辆 |
| 点击“查看记录” | 进入详细记录并自动应用该车辆筛选 |
| 修改车辆名称或车牌 | 所有记录详情、筛选、统计摘要和车辆选择器立即显示最新信息 |
| 修改能源类型 | 历史记录保持原样，后续记账按新车型适配规则显示补能场景 |
| 设置默认车辆 | 下次打开 App 及“全部车辆”状态下新增记录时优先选择该车 |
| 删除车辆 | 车辆和关联记录完整删除，全部统计立即重算，当前车辆及筛选安全回退 |

### 6.7 “更多”模块联动【V1.10 新增】

| “更多”入口或状态 | 关联模块结果 |
| --- | --- |
| 车辆管理 | 进入车辆管理；返回时回到“更多”，车辆数量和默认车辆摘要使用最新数据 |
| 能耗统计且已选具体车辆 | 进入该车辆的能耗统计，保留适用能源类型和现有时间范围规则 |
| 能耗统计且当前为全部车辆 | 进入能耗统计后要求选择一辆具体车辆，不跨车辆混算 |
| 能耗统计且无车辆 | 显示添加车辆的明确入口，不展示误导性零能耗 |
| 数据管理 | 直接打开现有备份与恢复区域，保留校验、合并、覆盖确认和失败保护 |
| 车辆新增、编辑、设置默认或删除 | 返回“更多”后车辆数量、默认车辆及能耗上下文立即更新 |
| 浏览器后退 | 返回“更多”并保持手机底部导航的当前项状态 |

## 7. 本地数据文件、备份和恢复要求

### 7.1 日常本地存储

- App 的工作数据保存在浏览器提供的本地持久化数据库中，推荐使用 IndexedDB；
- 浏览器之间的本地存储彼此隔离：在另一浏览器或浏览器用户配置文件中打开 App 时，不会自动看到原浏览器的数据；需要通过导出备份、导入恢复迁移数据；
- 不以浏览器临时内存作为唯一数据源；
- 每次新增、修改或删除后应立即持久化；
- 刷新页面、关闭浏览器、重新打开 App 或重启电脑后，数据保持不变；
- 数据不得自动上传到网络；
- App 应显示保存状态，并在保存失败时明确提示用户；
- 浏览器数据被用户主动清理或浏览器环境损坏不属于日常持久化保证范围，因此必须提供文件备份和恢复能力。

### 7.2 备份文件

- 用户可主动导出一份完整备份文件到电脑；
- 第一版备份文件采用 UTF-8 JSON 格式；
- 建议文件名：`car-expense-backup-YYYYMMDD-HHmmss.json`；
- 备份必须包含：格式版本、导出时间、车辆、全部记录、必要设置和数据标识；
- 备份不依赖网络或云服务；
- 导出后不删除或改变当前数据；
- 同一份有效备份应可以在清空后的 App 中恢复出相同车辆、记录和必要设置。

建议的备份顶层结构：

```text
schemaVersion
exportedAt
vehicles
records
settings
```

### 7.3 导入检查

导入前必须检查：

- 文件是否能正常读取；
- 是否为 App 支持的备份格式；
- 格式版本是否兼容；
- 必要数据字段是否存在；
- 车辆与记录的归属关系是否有效；
- 数据类型是否正确；
- 文件损坏或格式错误时不得写入任何数据。

导入确认前应向用户展示：备份时间、车辆数量、记录数量和导入模式。

### 7.4 恢复方式

第一版提供两种恢复方式：

- **合并导入**：保留当前数据，根据稳定唯一编号导入备份中不存在的数据，并用备份内容更新编号相同的数据；
- **覆盖恢复**：先用有效备份替换现有业务数据，再重新加载全部统计结果。

覆盖恢复属于高风险操作，必须二次确认。校验失败或导入过程中发生错误时，现有数据必须保持原样，不能出现只导入一部分的状态。

### 7.5 数据一致性

- 每条记录必须归属于一辆存在的车辆；
- 删除车辆时必须同时处理该车辆的全部记录；
- 金额、里程、加油量和充电量等数值以原始输入值保存，展示时再按单位格式化；
- 导入、编辑或删除后，首页和各统计模块应使用同一份最新数据重新计算；
- 备份格式必须包含版本号，便于后续版本兼容和迁移。

## 8. 第一版必须完成的功能

1. 车辆管理支持车辆摘要卡片、新增、编辑、切换、默认车辆唯一性、跨模块快捷入口、数据敏感字段保护、原子级联删除、失败保护及手机端单列布局。【V1.8 修改】
2. 支持燃油车、纯电动车和插电式混动车。
3. 支持确认范围内的十二类用车费用，并按补能、日常用车、车辆养护和其他场景分组选择。【V1.4 修改】
4. 支持普通费用、加油和充电三类差异化录入字段，并按车型限制不适用的补能场景。【V1.4 修改】
5. 支持新增、查看、编辑、删除和复制记录；复制必须经过预填表单确认，编辑或复制切换场景时清理不再适用的专用数据。【V1.7 修改】
6. 首页支持具体车辆和全部车辆视图、月份切换、核心费用指标、上月对比、最近六个月趋势、费用类别构成、车辆或能耗摘要、最近记录、快捷操作及数据提醒。【V1.3 修改】
7. 费用日历支持月份和车辆条件、月度摘要、每天金额与笔数、当天记录明细、指定日期记账及记录查看、编辑和删除。【V1.5 修改】
8. 数据分析支持统一的时间、车辆和类别筛选，以及核心指标、费用趋势、月份对比、类别与车辆构成、明细下钻、空状态和移动端响应式展示。【V1.6 修改】
9. 详细记录支持组合筛选、URL 条件保持、结果汇总、稳定排序、详情抽屉、加载更多、跨模块下钻及桌面表格和手机卡片两种布局。【V1.7 修改】
10. 能耗统计按车辆能源类型展示油耗、电耗或混动车的分项能耗和综合能源成本。
11. 记录发生变化后，首页、日历、分析、记录和能耗结果自动联动更新。
12. 数据在刷新、关闭浏览器和重启电脑后保持存在。
13. 支持导出完整 JSON 备份。
14. 支持校验备份并以合并或覆盖方式恢复。
15. 提供空状态、数据不足提示、表单校验、删除确认和保存状态反馈。
16. 桌面端左侧七个一级导航和手机端底部五个导航入口均显示匹配图标、文字及清晰当前页状态；手机端“更多”通过独立入口卡片访问能耗统计、车辆管理和数据管理，并显示准确的车辆或本地数据状态摘要。【V1.10 修改】
17. 加油记录的油品标号支持下拉选择常用油品。
18. 详细记录默认最早优先排序，且清除筛选后清除除全局车辆外的全部条件并恢复该默认排序。【V1.7 修改】
19. 用车记账支持入口条件自动带入、数量与单价双向计算、最高里程参考、字段级校验、重复提交保护、“保存并查看记录”和“保存并再记一笔”。【V1.4 新增】
20. 车辆管理在新增、编辑、设为默认或删除后，与首页、用车记账、费用日历、数据分析、详细记录和能耗统计实时保持一致。【V1.8 新增】
21. 首页、用车记账、数据分析和车辆管理按照四张正式视觉基准实现，并复用 V1.9 设计令牌、组件状态、响应式、可访问性、资源安全和视觉回归要求；既有功能与数据口径不得因视觉改造减少或改变。【V1.9 新增】
22. “更多”按照 V1.10 的信息分组、入口卡片、状态摘要、导航闭环和响应式要求实现；手机端不显示无关的全局车辆选择器或新增按钮，且不得改变现有模块业务和本地数据边界。【V1.10 新增】

## 9. 第一版暂时不做的功能

- 用户注册、登录和多账号；
- 云端数据库、多设备同步和云备份；
- 原生手机 App、应用商店安装包和移动端专属系统能力；本地 Web 版的手机浏览器响应式适配属于第一版范围。【V1.6 修改】
- 服务器、联网功能和正式部署；
- 社区、排行榜、记录分享和社交功能；
- 地图、行程轨迹和定位；
- 自动读取车辆、OBD 或车机数据；
- 自动连接加油站、充电平台、银行卡或支付账单；
- 发票识别、图片上传和 OCR；
- 保险、保养、年检等到期提醒；
- 预算管理、超支通知和消息推送；
- 多币种、英制单位和自定义计量单位；
- 多人共同管理同一辆车；
- Excel、CSV、PDF 等业务报表导出；第一版仅提供完整 JSON 数据备份；
- 自定义费用类别和复杂标签系统；
- 智能建议、预测费用或 AI 分析；
- 对油耗和电耗做不科学的直接合并。

### 9.1 首页总览本次暂时不做【V1.3 新增】

以下内容本次首页重设计不实现，但完整保留在 PRD 中，供后续版本继续评估和优化：

- 自定义首页组件及拖拽调整组件顺序；
- 用户自行选择显示或隐藏哪些指标卡；
- 预算设置、预算进度和超支提醒；
- 保养、保险、年检等到期提醒；
- 与同车型或其他用户的费用、油耗和电耗比较；
- 未来费用和能耗预测；
- 云端同步及多设备共享；
- 首页数据导出、打印或生成首页报告。

### 9.2 用车记账本次暂时不做【V1.4 新增】

以下内容本次用车记账重设计不实现，但完整保留在 PRD 中，供后续版本继续评估和优化：

- 拍照上传票据、维修单或其他附件；
- OCR 自动识别发票或小票；
- 银行卡、支付平台、加油站和充电平台账单导入；
- GPS 自动获取地点；
- 自动读取车辆、车机或 OBD 里程；
- 自定义费用类别、自定义记账模板和自定义字段；
- 将一张账单拆分为多个费用类别；
- 多笔批量录入；
- 周期性费用自动记账；
- 语音记账；
- 草稿跨浏览器保存；
- 维修配件、工时和保养项目明细；
- 保险、年检有效期及到期提醒；
- 预算判断和超支提示；
- 多币种和其他计量单位；
- AI 自动分类或智能填写。

### 9.3 费用日历本次暂时不做【V1.5 新增】

以下内容本次费用日历重设计不实现，但完整保留在 PRD 中，供后续版本继续评估和优化：

- 周视图和年视图；
- 多日期范围选择；
- 在日期格中展示完整记录列表；
- 按费用类别在日期格中绘制复杂饼图；
- 预算、账单提醒或未来计划；
- 拖拽记录修改发生日期；
- 节假日、农历和天气信息；
- 日历数据导出和打印；
- 自定义每周起始日。

### 9.4 数据分析本次暂时不做【V1.6 新增】

以下内容本次数据分析重设计不实现，但完整保留在 PRD 中，供后续版本继续评估和优化：

- 预算设置、预算进度和超支提醒；
- AI 分析、费用预测或智能建议；
- PDF、Excel、图片等分析报告导出；
- 不同用户或同车型费用排名；
- 跨车辆计算每公里成本；
- 将油耗和电耗合并为单一能耗指标；
- 自定义图表、拖拽布局和用户自定义分析看板。

### 9.5 详细记录本次暂时不做【V1.7 新增】

以下内容本次详细记录重设计不实现，但完整保留在 PRD 中，供后续版本继续评估和优化：

- 批量编辑或批量删除记录；
- Excel、CSV、PDF 等明细或业务报表导出；
- 自定义表格列和列顺序；
- 保存、命名或共享常用筛选方案；
- 自定义标签和复杂标签筛选；
- 票据、维修单或其他附件的上传和预览；
- 无限滚动和虚拟列表；
- 云端同步及多人协作。

### 9.6 车辆管理本次暂时不做【V1.8 新增】

以下内容本次车辆管理重设计不实现，后续版本根据实际使用价值、数据迁移成本和 App Store 上线影响再评估：

- 品牌、型号、年款、车辆识别代号 VIN 等扩展车辆档案；
- 购车日期、购车价格、油箱容量和电池容量；
- 车辆图片、行驶证、保险单或其他附件；
- 车辆归档、回收站和删除后撤销；
- 车辆分组、标签、搜索、批量操作和自定义排序；
- 保险、保养、年检等到期管理和通知；
- OBD、车机、厂商平台或第三方车辆数据同步；
- GPS 定位、行程轨迹和自动里程；
- 多人共同管理车辆或车辆资料云端同步；
- 对历史补能记录进行自动转换或跨能源类型重新解释。

### 9.7 UI 视觉系统本次暂时不做【V1.9 新增】

以下内容不属于本轮正式视觉基准实现范围，后续根据用户价值、性能和 App Store 上线影响再评估：

- 深色模式、用户自定义主题、换肤和自定义首页布局；
- 预算进度、费用预测、超支提醒和没有真实数据来源的通知红点；
- 3D 车辆模型、实时车辆动画、复杂视差和持续循环的装饰动效；
- 将车型插画与用户真实车型、颜色或车牌自动匹配；
- 运行时远程字体、远程图标、远程插画和必须联网才能显示的视觉资源；
- 为视觉还原引入大型 UI 框架、追踪 SDK 或与现有技术栈重复的组件库；
- 将视觉稿示例数据、图片中的文字或图表截图作为正式产品数据或 UI；
- 原生 iOS 工程和 SwiftUI 重写；本轮仅建立可迁移到后续 iOS App 的 Web 视觉规范，不扩大现阶段技术范围。

### 9.8 “更多”模块本次暂时不做【V1.10 新增】

以下内容不属于本次“更多”页面优化范围：

- 登录、账号、个人中心或多人协作；
- 云同步、云备份或远程存储；
- 消息中心、虚构通知数量或没有真实数据来源的提醒红点；
- 深色模式、主题切换或自定义入口排序；
- App 评分、分享、关于我们、在线客服或运营内容；
- 新增中转页、重复的数据管理页面或重复的用车记账悬浮按钮；
- 远程字体、远程图片、追踪 SDK、网络服务或新的系统权限；
- 数据库 schema、JSON 备份格式或车辆与记录业务字段变更。

## 10. 可实际检查的验收标准【V1.10 修改】

以下验收均应在不登录、不联网的电脑或手机浏览器环境中完成。【V1.7 修改】

| 编号 | 验收项目 | 可检查的通过条件 |
| --- | --- | --- |
| AC-01 | 页面框架【V1.10 修改】 | 桌面端可看到七个一级导航、车辆选择器、“记一笔”和数据管理；手机端可看到五个底部导航，并可从“更多”的独立入口卡片和页面快捷入口进入全部七个模块、用车记账及数据管理，不存在只在某一端可达的功能。 |
| AC-02 | 多车辆管理【V1.8 修改】 | 可分别创建燃油、纯电和插电式混动车；各车卡片正确显示车辆资料、当前里程、累计费用、记录数和最近记录日期，并可独立编辑、记账和查看记录。 |
| AC-03 | 默认车辆【V1.8 修改】 | 第一辆车自动成为默认车辆；存在车辆时始终有且只有一辆默认车辆；设置任意车辆为默认后重新打开 App，系统按规定优先选择该车辆。 |
| AC-04 | 普通费用记账【V1.4 修改】 | 可为指定车辆选择任一普通费用场景并新增记录；页面只显示通用字段，当前里程可留空；保存后详情中的车辆、类别、金额、日期、地点和备注与输入一致。 |
| AC-05 | 加油记账【V1.4 修改】 | 燃油车和插混车可保存包含金额、加油量、当前里程、是否加满和油品标号的记录；油品标号使用规定的下拉选项；数量与单价可计算金额，金额与数量可计算参考单价，手动修改后计算结果正确。 |
| AC-06 | 充电记账【V1.4 修改】 | 纯电动车和插混车可保存包含金额、充电量、当前里程、是否充满和充电方式的记录；充电方式使用规定的下拉选项；数量、单价和金额之间的计算规则与加油一致。 |
| AC-07 | 场景化表单与车型适配【V1.4 修改】 | 十二类费用按四组场景显示且有清晰选中状态；普通费用不显示补能字段；燃油车不提供充电、纯电动车不提供加油、插混车同时提供两者；切换为不适用车辆时出现提示并要求重新选择。 |
| AC-08 | 表单校验【V1.4 修改】 | 未选车辆、金额不大于 0、日期时间为空、数值无效或补能缺少数量及里程时不能保存；错误显示在对应字段附近并聚焦第一个错误；里程低于历史最高值时说明统计影响，取消不保存、确认后可保存。 |
| AC-09 | 首页联动【V1.3 修改】 | 新增、编辑、删除或导入记录后无需刷新，首页当前条件下的费用、记录数、趋势、类别、车辆或能耗摘要及最近记录立即重新计算且数值正确。 |
| AC-10 | 首页跳转【V1.3 修改】 | 点击首页指标、趋势月份或费用类别后进入详细记录，并自动应用对应车辆、月份和类别筛选；点击能耗摘要进入当前车辆的能耗统计。 |
| AC-11 | 费用日历基础联动【V1.5 修改】 | 新增一笔指定日期的费用后，该日期及当月摘要立即显示正确金额和记录数；点击该日期可看到对应记录，无需刷新页面。 |
| AC-12 | 数据分析基础联动【V1.6 修改】 | 选择时间、车辆和类别后，核心指标、趋势、月份对比、类别构成和车辆对比使用同一条件；点击月份、趋势、类别或车辆可进入条件一致的详细记录。 |
| AC-13 | 详细记录筛选【V1.7 修改】 | 按车辆、关键词、日期、类别和金额组合筛选后，只显示符合条件的记录，页面内车辆与顶部全局车辆条件一致，数量和金额合计与全部筛选结果相符。 |
| AC-14 | 搜索与排序【V1.7 修改】 | 输入类别、车辆、商家、地点或备注关键词可找到对应记录；四种日期或金额排序结果正确，主字段相同时顺序稳定。 |
| AC-15 | 查看与编辑【V1.7 修改】 | 点击记录主体先打开完整详情；进入编辑并保存后返回原查看或列表上下文，筛选、排序、已加载数量及可继续操作的位置保持，创建时间不变、修改时间更新；切换类别后不适用的补能专用数据被清除。 |
| AC-16 | 删除联动【V1.7 修改】 | 删除确认显示日期、车辆、类别和金额；取消时数据不变，确认后记录同时从首页、日历、分析、详细记录和相关能耗计算中消失；删除失败时原记录保留并显示错误信息。 |
| AC-17 | 能耗分车型与筛选【V1.2 修改】 | 选择燃油车、纯电动车或插混车时，页面分别展示适用指标；默认显示近十二个月，并可切换时间范围；选择“全部车辆”时要求选择具体车辆。 |
| AC-18 | 能耗数据不足与质量提示【V1.2 修改】 | 缺少里程、数量、完整加满或充满边界，或全部记录被排除时，页面显示具体原因和处理入口，不显示 0 能耗或空图表。 |
| AC-19 | 完整区间与加权计算【V1.2 修改】 | 两条完整补能边界能够形成区间，期间部分补能的数量和费用累计计入；平均能耗按总补能量和总有效里程加权计算，并能查看参与计算的全部原始记录。 |
| AC-20 | 能耗趋势与记录联动【V1.2 修改】 | 每个趋势点可查看区间起止里程、距离、补能量、能耗和成本；编辑、排除或恢复补能记录后，指标、趋势和区间明细立即重新计算，刷新后状态仍然保留。 |
| AC-21 | 全部车辆视角 | 选择“全部车辆”后，首页和数据分析能显示跨车辆汇总；进入能耗统计时要求选择具体车辆。 |
| AC-22 | 车辆删除保护【V1.8 修改】 | 删除确认显示车辆名称、关联记录数、关联费用及影响模块；有记录时进行加强确认；取消时数据不变，确认后车辆和记录完整删除，失败时不得出现部分删除。 |
| AC-23 | 刷新持久化 | 新增车辆和记录后刷新浏览器，数据仍然存在且内容一致。 |
| AC-24 | 重启持久化 | 关闭浏览器并重新打开 App，或重启电脑后再次打开，已保存数据仍然存在。 |
| AC-25 | 保存反馈【V1.4 修改】 | 新增或编辑时可看到保存中和保存成功状态；保存期间按钮禁用且连续点击只产生一条记录；模拟保存失败时显示失败原因，不跳转、不清空已填内容且不能误报为已保存。 |
| AC-26 | 导出备份 | 导出后得到可读取的 JSON 文件，文件包含格式版本、导出时间、全部车辆、全部记录和必要设置，且当前数据不受影响。 |
| AC-27 | 无效备份保护 | 选择损坏文件、普通 JSON 或缺少必要字段的文件时，系统拒绝导入、说明原因，现有数据完全不变。 |
| AC-28 | 合并导入 | 在现有数据上合并一份有效备份后，不同编号的数据被加入，相同编号的数据按备份更新，且不产生重复记录。 |
| AC-29 | 覆盖恢复 | 覆盖前出现二次确认；确认后当前业务数据被备份数据完整替换，车辆数、记录数和关键内容与备份一致。 |
| AC-30 | 导入原子性 | 导入过程中发生校验或写入错误时，不出现只恢复部分车辆或部分记录的状态，原有数据仍可正常使用。 |
| AC-31 | 空状态 | 无车辆、无记录、筛选无结果和能耗数据不足时，分别显示与当前场景相符的说明及下一步操作。 |
| AC-32 | 本地运行边界 | 断开网络后，车辆管理、记账、日历、分析、记录、能耗、导出和导入仍能正常使用，不请求登录或云服务。 |
| AC-33 | 导航识别【V1.9 修改】 | 桌面端左侧七个一级导航与手机端底部五个入口均使用统一风格的匹配图标和文字；当前页状态不只依赖颜色；专注表单按规定隐藏底部导航并提供可用的返回操作。 |
| AC-34 | 记录默认排序与重置【V1.7 修改】 | 进入详细记录时按最早发生日期优先排列；切换为其他排序及设置任意筛选后，点击“清除全部”会清除关键词、类别、日期和金额条件，恢复“最早优先”，并继续与顶部全局车辆条件保持一致。 |
| AC-35 | 跨浏览器迁移边界 | 在另一浏览器打开 App 时不要求自动共享数据；通过导出原浏览器备份并在另一浏览器导入后，车辆、记录和必要设置可恢复。 |
| AC-36 | 首页车辆与月份条件【V1.3 新增】 | 首页默认显示当前月；可切换上月、下月并回到本月。切换具体车辆、全部车辆或月份后，页面全部指标和摘要使用同一条件重新计算。 |
| AC-37 | 首页核心指标【V1.3 新增】 | 具体车辆视图正确显示月费用、记录数、平均每笔、单公里综合成本和上月变化；全部车辆视图用有记录车辆数替代单公里成本，且不跨车辆混算里程或能耗。 |
| AC-38 | 首页趋势与类别【V1.3 新增】 | 最近六个月费用趋势与对应月份记录合计一致；本月类别金额之和等于本月总费用；点击月份或类别后详细记录自动应用正确筛选。 |
| AC-39 | 首页车辆与能耗摘要【V1.3 新增】 | 具体车辆按能源类型显示适用的最近及平均能耗摘要；插混车不显示无科学依据的综合能耗；全部车辆显示各车辆本月费用、记录数和占比。 |
| AC-40 | 首页最近记录与快捷操作【V1.3 新增】 | 首页显示最近五条记录；点击后可查看详情并进入编辑。快捷记账、加油或充电能自动带入当前车辆及适用类别。 |
| AC-41 | 首页提醒与空状态【V1.3 新增】 | 无车辆、无任何记录、当前月无记录及数据质量不足时分别显示符合场景的说明和下一步入口；数据不足时不显示误导性单公里成本或能耗零值。 |
| AC-42 | 首页响应式与键盘操作【V1.3 新增】 | 窄屏浏览器中首页内容自动改为两列或单列且页面无横向滚动；趋势点、类别、车辆对比和最近记录均可通过键盘操作，状态不只依赖颜色表达。 |
| AC-43 | 记账页面结构【V1.4 新增】 | 页面按车辆、场景、场景化表单和保存操作组织；金额突出显示，补充信息层级清楚，桌面浏览器中内容宽度适合阅读。 |
| AC-44 | 车辆自动带入和空状态【V1.4 新增】 | 当前具体车辆或默认车辆能够自动带入，并显示能源类型和最高里程参考；无车辆时不显示空白表单，新增车辆后可返回继续记账。 |
| AC-45 | 入口条件带入【V1.4 新增】 | 从首页普通记账、记录加油或记录充电、费用日历进入时，分别正确带入适用的车辆、类别或日期；不适用车型不会接受错误的补能预设。 |
| AC-46 | 补能计算与说明【V1.4 新增】 | 加油或充电的数量和单价可自动得到金额，金额和数量可得到参考单价；以最后主动修改字段为准重新计算，精度正确；页面说明里程和加满或充满状态对能耗计算的影响。 |
| AC-47 | 保存后操作【V1.7 修改】 | “保存并查看记录”进入详细记录，刚保存记录自动进入可视区域并短暂高亮；“保存并再记一笔”保留车辆和类别，清空本次业务数据并将发生时间更新为当前时间。 |
| AC-48 | 记账模块联动【V1.4 新增】 | 保存普通费用后首页、日历、分析和详细记录立即更新；保存补能记录后能耗统计也立即更新；无需刷新浏览器。 |
| AC-49 | 数据清理和失败保护【V1.4 新增】 | 加油、充电和普通费用之间切换并保存后，记录中只保留当前场景适用的数据；校验或保存失败后所有已填内容保持不变。 |
| AC-50 | 记账键盘操作【V1.4 新增】 | 用户可仅用键盘完成车辆选择、场景选择、字段填写和保存；选中、错误、保存中及成功状态不只依赖颜色表达。 |
| AC-51 | 日历月份与车辆条件【V1.5 新增】 | 进入日历默认显示当前月；切换上月、下月、指定月份、具体车辆或全部车辆后，月合计、记录数、有费用天数及每天数据均正确，当前条件在页面操作期间保持一致。 |
| AC-52 | 日期格数据与状态【V1.5 新增】 | 有记录日期正确显示金额和笔数，无记录日期不显示零金额；今天和选中日期状态清晰且可区分，金额和记录数不会造成日期格重叠或溢出。 |
| AC-53 | 当天明细【V1.5 新增】 | 点击日期后显示当天合计和全部记录并按发生时间从早到晚排列；全部车辆模式正确显示每笔记录所属车辆；无记录日期显示明确空状态。 |
| AC-54 | 从日历新增记录【V1.5 新增】 | 从今天或指定日期记账时正确带入日期和车辆；发生时间使用当前时间且目标日期不变；保存后返回原日历上下文并立即更新对应日期和当月摘要。 |
| AC-55 | 日历查看与编辑联动【V1.5 新增】 | 可从当天记录打开详情和编辑；修改金额、日期、车辆或类别后，原日期、目标日期和月份摘要立即按当前条件重新计算，并保留可继续操作的日历上下文。 |
| AC-56 | 日历删除联动【V1.5 新增】 | 从日历删除记录前出现确认；确认后当天金额、笔数、有费用天数和当月合计立即更新，取消删除时所有数据不变。 |
| AC-57 | 日历空状态【V1.5 新增】 | 无车辆、无任何记录、当前月份无记录和选中日期无记录时，分别显示符合场景的说明及新增车辆或记账入口，不出现不可操作的空白区域。 |
| AC-58 | 日历响应式与键盘操作【V1.5 新增】 | 常见桌面及窄屏宽度下，月历和当天明细无重叠且页面无横向滚动；日期和记录可使用键盘访问，抽屉焦点进入和返回正确，状态不只依赖颜色表达。 |
| AC-59 | 分析筛选和条件保持【V1.6 新增】 | 本月、近三个月、今年、全部时间和有效自定义范围计算边界正确；车辆和类别切换后全部结果同步更新；进入详细记录再返回时原条件保持，清除筛选恢复规定默认值。 |
| AC-60 | 分析核心指标【V1.6 新增】 | 总费用、记录数、按覆盖自然月计算的平均每月费用、上一周期变化和最高费用类别结果正确；无可比数据时不显示误导百分比。 |
| AC-61 | 分析费用趋势【V1.6 新增】 | 不超过 31 天时按日、超过 31 天时按月展示连续趋势，零费用时段正确补零；趋势点金额和记录数正确且可下钻至对应详细记录。 |
| AC-62 | 分析月份对比【V1.6 新增】 | 各月金额、记录数、环比金额和百分比正确；首月或上月费用为零时显示暂无可比数据；点击月份带入正确条件。 |
| AC-63 | 分析类别构成【V1.6 新增】 | 类别按金额降序展示金额、记录数和占比，各类别金额之和等于总费用；单类别模式不显示误导性多类别构成，点击类别下钻条件正确。 |
| AC-64 | 分析车辆对比和单车成本【V1.6 新增】 | 全部车辆模式正确展示各车金额、记录数和占比且不混算里程；具体车辆模式仅在里程条件有效时显示正确每公里成本，否则说明具体原因。 |
| AC-65 | 分析空状态和无效条件【V1.6 新增】 | 无车辆、无任何记录、筛选无结果、自定义日期无效和局部指标数据不足时分别显示准确说明及可执行的下一步，不生成空图或错误零值。 |
| AC-66 | 分析实时联动【V1.6 新增】 | 新增、编辑、删除或导入记录后，当前分析条件下的指标、趋势、月份、类别和车辆结果无需刷新即使用最新数据。 |
| AC-67 | 分析响应式与可访问性【V1.6 新增】 | 常见桌面、iPhone 和 Android 浏览器宽度下内容无重叠、裁切或整体横向滚动；横竖屏、触控和键盘操作有效，图表有文字数据且状态不只依赖颜色。 |
| AC-68 | 详细记录条件与 URL【V1.7 新增】 | 车辆、关键词、类别、日期范围、金额范围和排序可组合使用并写入 URL；刷新、前进、后退或重新打开地址后条件和结果一致；无效参数安全回退且页面不报错。 |
| AC-69 | 详细记录条件校验【V1.7 新增】 | 开始日期晚于结束日期、最低金额高于最高金额、金额为负数或无效数字时显示对应提示且不执行错误筛选，修正后结果恢复正常。 |
| AC-70 | 详细记录结果汇总【V1.7 新增】 | 记录数、总金额、平均单笔金额和结果日期范围均基于全部筛选结果且计算正确；加载更多不改变汇总；无结果时显示规定的零值或占位符，不出现异常数值。 |
| AC-71 | 详细记录列表与稳定排序【V1.7 新增】 | 记录显示规定的主要信息和适用补能字段；四种排序结果正确；主排序字段相同时，以时间和记录编号稳定排序，重新渲染后不会无故跳动。 |
| AC-72 | 详细记录响应式列表【V1.7 新增】 | 桌面宽度使用可操作的表格，常见 iPhone 和 Android 宽度使用单列卡片；主要信息不被裁切，页面主体无横向滚动，触控操作区域符合要求。 |
| AC-73 | 详细记录详情与焦点【V1.7 新增】 | 点击记录主体只打开详情抽屉并显示全部适用字段；打开后焦点进入抽屉，关闭后返回原记录，键盘和触控均可完成查看及后续操作。 |
| AC-74 | 详细记录编辑上下文【V1.7 新增】 | 编辑成功后列表和汇总立即更新并保持筛选、排序、已加载数量和列表上下文；记录不再符合条件时正确移出并提示成功；失败时保留表单内容，取消时原数据不变。 |
| AC-75 | 详细记录删除安全【V1.7 新增】 | 删除必须经过包含关键记录信息的确认；成功后全部关联模块立即更新且列表可继续操作；取消不改变数据；模拟失败时记录保留并显示可理解的错误。 |
| AC-76 | 复制为新记录【V1.7 新增】 | 点击复制只打开预填的新记录表单，不立即创建数据；新记录使用新编号和当前时间，允许修改全部业务字段；取消不产生记录，确认保存后仅新增一条并定位高亮。 |
| AC-77 | 详细记录加载更多【V1.7 新增】 | 首次最多展示 30 条，每次再展示 30 条，并正确显示“已展示 X / 共 Y 条”；全部展示后隐藏按钮；筛选或排序变化后恢复首次展示数量。 |
| AC-78 | 详细记录定位高亮【V1.7 新增】 | 从“保存并查看记录”或复制保存返回时，目标记录在当前条件下自动进入可视区域并短暂高亮，高亮结束后记录仍可正常操作且状态不只依赖颜色。 |
| AC-79 | 详细记录跨模块联动【V1.7 新增】 | 从首页、费用日历和数据分析的规定入口下钻时，车辆、日期、类别等条件映射正确，详细记录数量和总金额与来源一致；返回来源页面后原筛选条件保持。 |
| AC-80 | 详细记录空状态与可访问性【V1.7 新增】 | 无车辆、无任何记录、筛选无结果、加载失败和操作失败时分别显示准确说明及可执行入口；筛选、记录、抽屉和操作菜单支持键盘与触控，错误、选中、高亮和禁用状态不只依赖颜色。 |
| AC-81 | 车辆管理页面结构【V1.8 新增】 | 页面显示车辆数量、新增车辆和备份与恢复入口；车辆以卡片展示，默认车辆置顶，其余顺序稳定；常见桌面及手机宽度下无重叠、裁切或整体横向滚动。 |
| AC-82 | 车辆卡片摘要【V1.8 新增】 | 每辆车正确显示名称、默认状态、已填写的车牌、能源类型、当前里程、累计费用、记录数和最近记录日期；无记录时使用初始里程并显示零费用和暂无记录。 |
| AC-83 | 当前里程计算【V1.8 新增】 | 有有效里程记录时取最高有效里程，无有效记录时取初始里程；缺失、非数字和异常里程不参与计算且不会导致页面报错。 |
| AC-84 | 新增车辆校验【V1.8 新增】 | 名称、能源类型、初始里程和车牌按规定完成必填、长度、数值及精度校验；错误显示在对应字段附近，数据无效时不能保存；重复名称或车牌仅提示并允许确认后继续。 |
| AC-85 | 新增车辆保存保护【V1.8 新增】 | 第一辆车自动设为默认；保存期间按钮禁用且连续操作只产生一辆车；保存成功后列表立即更新，模拟失败时保留全部输入、不产生不完整车辆且不误报成功。 |
| AC-86 | 车辆普通字段联动【V1.8 新增】 | 修改车辆名称或车牌并保存后，车辆管理、车辆选择器、记录详情、费用日历、数据分析、详细记录和能耗统计无需刷新即显示最新信息。 |
| AC-87 | 初始里程修改保护【V1.8 新增】 | 有历史记录时，初始里程高于最低有效历史里程会被阻止并显示原因；可能影响展示时保存前出现说明；校验或保存失败时任何车辆字段均不发生部分更新。 |
| AC-88 | 能源类型修改保护【V1.8 新增】 | 有关联补能记录时修改能源类型必须经过影响确认；取消时数据不变，确认后历史记录不被删除、转换或覆盖，后续记账按新能源类型提供适用场景。 |
| AC-89 | 未保存内容保护【V1.8 新增】 | 修改车辆表单后关闭、返回或离开时出现未保存提示；继续编辑保留全部输入，放弃后原车辆不变；无修改或保存成功时不产生多余提醒。 |
| AC-90 | 默认车辆一致性【V1.8 新增】 | 设置新默认车辆后原默认状态自动取消并即时反馈；设置失败时状态保持；设置操作不强制跳转用户当前页面或中断正在处理的其他车辆。 |
| AC-91 | 车辆快捷入口【V1.8 新增】 | 从车辆卡片点击“记一笔”进入记账并带入对应车辆；点击“查看记录”进入详细记录并应用该车辆筛选；用户仍可按规定切换或清除车辆条件。 |
| AC-92 | 车辆删除一致性【V1.8 新增】 | 删除车辆与全部关联记录作为完整操作执行；成功后所有关联模块立即重算，失败时车辆、记录和默认状态均保持原样，且显示可理解的错误。 |
| AC-93 | 删除默认或最后车辆【V1.8 新增】 | 删除默认车辆且仍有其他车辆时自动建立一个新默认车辆；删除最后一辆车后进入无车辆状态，当前车辆和筛选安全清除，各模块不崩溃且不保留无效引用。 |
| AC-94 | 车辆管理移动端与可访问性【V1.8 新增】 | 常见 iPhone 和 Android 浏览器宽度下使用单列卡片，主要触控区域约 44×44 像素或以上；软键盘、安全区域、长文本、焦点返回和辅助状态提示均可正常使用，危险和错误状态不只依赖颜色。 |
| AC-95 | 正式视觉基准还原【V1.9 新增】 | 首页、用车记账、数据分析和车辆管理在 390px 宽手机视口下，与四张正式视觉基准的信息顺序、主次层级、冷雾灰白背景、青绿色主操作、白色圆角卡片和柔和阴影一致；页面使用真实组件和真实数据，不以整图充当 UI。 |
| AC-96 | 设计令牌一致性【V1.9 新增】 | 抽查四个基准页面及至少两个既有页面，颜色、字体、间距、圆角、阴影和图标来自统一令牌或公共组件；不存在无业务理由的近似色、随机圆角、混用图标或远程字体。 |
| AC-97 | 手机端导航闭环【V1.10 修改】 | 在 320px、375px、390px、414px 和 430px 宽度下，底部导航顺序固定为首页、费用日历、详细记录、数据分析、更多；“更多”以独立卡片提供能耗统计、车辆管理和数据管理，用车记账从规定快捷入口到达；当前项和返回路径清楚。 |
| AC-98 | 桌面与平板响应式【V1.9 新增】 | 在 768px、1024px、1280px 和 1440px 宽度下，导航位置符合断点规则，内容在单列、双列或多列间平稳调整；无整体横向滚动、遮挡、异常超宽卡片或页面切换时导航跳变。 |
| AC-99 | 首页视觉与功能完整性【V1.9 新增】 | 首页首屏可完成车辆和月份确认、查看本月费用及记一笔，并正确显示记录数、当前里程、趋势和最近记录；费用构成、比较、能耗、提醒等既有内容仍可访问；装饰短线不表现预算语义，提醒红点只在真实提醒存在时出现。 |
| AC-100 | 用车记账视觉与表单可用性【V1.9 新增】 | 手机端记账页按车辆、场景、金额、日期与里程、专用字段、补充信息和保存顺序展示；底部双保存操作不被软键盘或安全区域遮挡；所有类别可达，错误就近显示，保存失败保留输入。 |
| AC-101 | 数据分析视觉与图表可用性【V1.9 新增】 | 数据分析按车辆、筛选、指标、趋势、类别构成和下钻组织；首屏三个指标与下方其余既有指标均正确；趋势和类别图使用真实数据并提供等价文字列表，加载、无数据、数据不足和失败状态可区分。 |
| AC-102 | 车辆管理视觉与操作安全【V1.9 新增】 | 车辆卡片正确展示默认状态、能源类型、车牌、当前里程、累计费用和记录数；记一笔与查看记录常驻，编辑、设为默认和删除可从更多操作访问；删除保持危险分隔和既有确认规则。 |
| AC-103 | 通用组件状态【V1.9 新增】 | 主按钮、次按钮、危险按钮、输入框、选择器、卡片和分段控件均可实际触发并识别默认、悬停或按下、键盘焦点、禁用、加载、错误及恢复状态；加载时不重复提交，失败时不误报成功。 |
| AC-104 | 对比度与非颜色表达【V1.9 新增】 | 使用自动化工具或人工测量确认普通文字对比度至少 4.5:1，大号文字和关键 UI 图形至少 3:1；选中、增长、下降、默认、警告、错误和危险状态均有文字、图标、形状或边框辅助表达。 |
| AC-105 | 触控、缩放、键盘与安全区域【V1.9 新增】 | 主要触控目标约 44×44px 或以上；浏览器 200% 文字缩放、手机横竖屏、软键盘弹出及带安全区域设备上，关键内容和操作不被裁切或遮挡；键盘焦点顺序合理且焦点轮廓可见。 |
| AC-106 | 视觉资源、安全与降级【V1.9 新增】 | 装饰车辆或城市资源为经过授权的本地独立文件，不含文字、真实用户信息或 Secret，并从辅助技术隐藏；断网、资源加载失败和减少动态效果开启时，核心内容、图表和操作仍完整可用。 |
| AC-107 | 视觉回归证据【V1.9 新增】 | 自动化测试至少生成首页、用车记账、数据分析和车辆管理在 390px 与 1280px 宽度的截图，并覆盖一个空状态和一个错误或加载状态；人工逐项对照正式基准和本章规范，差异有记录且无阻断级视觉回归。 |
| AC-108 | Release Impact Check【V1.9 新增】 | UI 改造完成后确认 Debug 构建、Production 构建、离线运行、现有数据与备份兼容、权限、隐私、Signing、Entitlements、性能和 GitHub Secret 扫描结果；本轮不新增数据迁移、网络依赖、系统权限或敏感配置。 |
| AC-109 | “更多”页面结构【V1.10 新增】 | 手机端页面显示“更多”和“管理车辆、能耗与本地数据”，按“车辆与用车”“数据与安全”分组展示三个独立入口卡片；不再显示全局车辆选择器和单独“+”按钮，不存在文字粘连或异常固定高度造成的大面积无效留白。 |
| AC-110 | 入口卡片视觉与状态【V1.10 新增】 | 车辆管理、能耗统计和数据管理均使用独立卡片，正确显示统一线性图标、标题、说明、真实状态摘要和方向箭头；默认、悬停、按下、键盘焦点及数据不足状态清晰，未出现 Emoji、字体字符或浏览器默认链接样式。 |
| AC-111 | 车辆管理入口【V1.10 新增】 | 有车辆时卡片显示正确车辆数量和默认车辆摘要，点击进入车辆管理；无车辆时显示“还没有车辆”和添加第一辆车的行动提示；新增、编辑、设置默认或删除后返回，摘要无需刷新即更新。 |
| AC-112 | 能耗统计入口【V1.10 新增】 | 已选具体车辆时显示正确车辆和适用能耗上下文；全部车辆状态提示进入后选择具体车辆；无车辆时说明添加车辆后可使用；任何状态均不跨车辆混算或用零值冒充无数据。 |
| AC-113 | 数据管理入口【V1.10 新增】 | 卡片显示“导出备份、导入与恢复本地数据”和“仅保存在本机”，点击直接进入现有数据管理区域；备份校验、合并、覆盖确认、失败保护和本地数据边界保持不变。 |
| AC-114 | “更多”导航与返回【V1.10 新增】 | 手机端底部导航保持可见并以非纯颜色方式选中“更多”；三个入口均可到达正确页面，浏览器后退回到“更多”且不形成循环；桌面直接访问时页面正常显示但不新增桌面一级导航。 |
| AC-115 | “更多”响应式布局【V1.10 新增】 | 320px、375px、390px、414px、430px、768px、1024px和1280px宽度下无整体横向滚动、遮挡或文字重叠；390px首屏显示标题和至少两个完整入口卡片；长车辆名称和200%文字缩放不会挤压图标或关键操作。 |
| AC-116 | “更多”空状态与异常【V1.10 新增】 | 无车辆、全部车辆、加载中和读取失败分别显示准确说明与可执行下一步；单个入口失败不导致整页崩溃，不以空白卡片或错误零值代替失败状态，断网时仍能进入现有本地功能。 |
| AC-117 | “更多”可访问性【V1.10 新增】 | 卡片和底部导航主要触控目标至少约 44×44px，普通文字对比度至少4.5:1；键盘焦点、阅读顺序、安全区域和辅助技术名称正确，图标、状态和当前导航项均不只依赖颜色表达。 |
| AC-118 | “更多”Release Impact Check【V1.10 新增】 | Debug、Production Build、离线模式、现有车辆与记录、当前及默认车辆、JSON备份兼容和 GitHub Secret 检查均通过；本轮未新增数据库迁移、网络服务、云同步、远程资源、权限、追踪 SDK、Signing 或 Entitlements。 |

## 11. 第一版核心流程

### 11.1 首次使用

1. 打开 App；
2. 创建第一辆车；
3. 自动将该车设为默认车辆；
4. 进入首页空状态；
5. 点击“记一笔”；
6. 选择记账场景并填写表单；
7. 保存后返回首页或查看记录；
8. 首页和各模块自动显示最新数据。

### 11.2 日常记账【V1.4 修改】

1. 从顶部“记一笔”、首页快捷入口或费用日历进入；
2. 确认系统带入的车辆，或在页面内切换车辆；
3. 按补能、日常用车、车辆养护或其他分组选择费用场景；
4. 填写金额、日期时间及当前场景所需的数据；补能时确认数量、里程及加满或充满状态；
5. 查看自动计算结果和最高里程参考，按字段提示修正问题；
6. 选择“保存并查看记录”或“保存并再记一笔”；
7. 保存成功后，在首页、日历、分析、详细记录及适用的能耗统计中查看同步结果。

### 11.3 备份与恢复

1. 用户进入“数据管理”；
2. 导出完整备份文件并自行妥善保存；
3. 需要恢复时选择备份文件；
4. App 校验并展示备份摘要；
5. 用户选择合并导入或覆盖恢复；
6. 覆盖恢复时二次确认；
7. 成功后重新加载全部车辆、记录和统计，并显示恢复结果。

---

本 PRD 是第一版设计和开发的范围依据。后续如增加功能，应先更新本文档的范围、数据要求和验收标准，再进入实现。
