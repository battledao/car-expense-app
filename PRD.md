# 车主用车记账 App 产品需求文档（PRD）

> 产品名称：暂定“爱车小精灵”  
> 文档版本：V1.1
> 状态：第一版需求已确认  
> 更新日期：2026-08-30  
> 适用范围：电脑浏览器本地版

## 1. 产品目标和使用场景

### 1.1 产品目标

为个人车主提供一个专门记录和分析用车支出的本地 App，让用户能够：

- 管理多辆车，并明确每笔记录属于哪辆车；
- 快速记录加油、充电、停车、保养、保险等实际用车费用；
- 从日期、费用类别、车辆和时间趋势等角度查看支出；
- 分别了解燃油车、纯电动车和插电式混动车的油耗、电耗及能源成本；
- 在不登录、不联网的情况下长期保存数据；
- 通过导出和导入备份文件保护及恢复本地数据。

第一版的重点是“记录方便、数据清楚、本地可靠”，不追求社交、联网或复杂自动化能力。

### 1.2 目标用户

- 拥有一辆或多辆汽车的个人车主；
- 希望了解日常养车成本，但不想使用通用财务软件的用户；
- 希望所有数据只保存在自己电脑上的用户；
- 使用燃油车、纯电动车或插电式混动车的用户。

### 1.3 主要使用场景

1. 用户加油、充电或产生停车等费用后，快速记一笔。
2. 用户回顾某一天或某个月发生过哪些用车费用。
3. 用户查看本月总费用、主要费用类别及近期支出变化。
4. 用户比较不同月份或不同车辆的用车成本。
5. 用户通过带里程的补能记录查看车辆油耗、电耗和每公里能源成本。
6. 用户新增、切换或维护多辆车的资料。
7. 用户定期导出完整备份，并在数据异常、浏览器数据被清理或更换环境时导入恢复。

### 1.4 第一版使用环境

- 只在用户自己的电脑浏览器中本地运行；
- 以桌面端 Chrome、Edge 等 Chromium 浏览器为主要验收环境；
- 不要求登录、联网、服务器、云数据库或部署；
- 默认使用人民币、公里、升和千瓦时作为金额及计量单位；
- 刷新页面、关闭浏览器和重启电脑后，已保存数据必须仍然存在。

## 2. 整体页面框架和导航结构

### 2.1 页面框架

第一版采用“左侧固定导航 + 顶部全局栏 + 中间内容区”的桌面结构。

```text
┌──────────────────────────────────────────────────────────────┐
│ 当前车辆：粤A·12345 ▼              ＋记一笔    数据管理      │
├──────────────┬───────────────────────────────────────────────┤
│ 首页总览     │                                               │
│ 用车记账     │              当前页面内容                      │
│ 费用日历     │                                               │
│ 数据分析     │                                               │
│ 详细记录     │                                               │
│ 能耗统计     │                                               │
│ 车辆管理     │                                               │
│              │                                               │
│ 本地保存状态 │                                               │
└──────────────┴───────────────────────────────────────────────┘
```

### 2.2 左侧主导航

左侧导航固定包含七个一级入口：

1. 首页总览
2. 用车记账
3. 费用日历
4. 数据分析
5. 详细记录
6. 能耗统计
7. 车辆管理

各模块必须解决不同问题，不允许仅复用同一任务列表并更换标题。

为提升扫描效率，每个一级导航名称左侧应显示与模块场景匹配的简洁图标；图标与文字共同构成可点击导航项，不能只依赖颜色区分当前页面。

### 2.3 顶部全局栏

顶部全局栏包含：

- 当前车辆选择器；
- “全部车辆”选项；
- “记一笔”快捷按钮；
- “数据管理”入口，用于导出备份和导入恢复；
- 本地保存状态，例如“正在保存”“已保存”“保存失败”。

选择车辆后，首页、费用日历、数据分析和详细记录应同步应用该车辆条件。能耗统计只能针对一辆具体车辆；从“全部车辆”进入时，应提示选择车辆。

### 2.4 统一交互形式

- **独立页面**：用于浏览首页、日历、分析、记录列表、能耗和车辆管理等大量内容；
- **右侧抽屉**：用于查看或编辑单条记录，关闭后保留用户原来的页面、筛选和滚动位置；
- **确认弹窗**：只用于删除、覆盖导入等高风险操作；
- **即时反馈**：新增、修改、删除或导入后，给出明确的成功或失败反馈；
- **离开提醒**：表单存在未保存修改时，关闭抽屉或离开页面需提醒用户。

## 3. 首页的内容结构

首页用于回答三个问题：最近花了多少钱、钱主要花在哪里、当前车辆最近发生了什么。

### 3.1 顶部概要

至少显示以下指标：

- 当前车辆，或“全部车辆”；
- 本月费用；
- 本月记录数；
- 平均每笔费用；
- 单公里成本；
- 当前车辆的平均油耗、电耗或综合能源成本摘要。

当缺少里程或有效能耗数据时，不能显示误导性的 0，应显示“数据不足”并说明需要补充什么记录。

### 3.2 费用摘要

- 最近若干月的费用趋势；
- 本月各费用类别的金额或占比；
- 本月费用与上月的变化；
- 全部车辆模式下的车辆费用摘要。

### 3.3 最近记录

- 显示最近发生的记录；
- 每条至少显示日期、车辆、类别和金额；
- 点击记录后，在右侧抽屉查看完整信息；
- 提供“查看全部记录”入口。

### 3.4 能耗摘要

- 燃油车显示最近油耗及平均油耗；
- 纯电动车显示最近电耗及平均电耗；
- 插电式混动车分别显示油耗、电耗，并可显示综合每公里能源成本；
- 点击摘要进入对应车辆的能耗统计页。

### 3.5 快捷操作

- 记一笔；
- 快速进入加油记录；
- 快速进入充电记录；
- 无车辆时优先引导“添加第一辆车”；
- 有车辆但无记录时引导“记录第一笔费用”。

## 4. 每个模块的用途

| 模块 | 主要用途 | 主要回答的问题 |
| --- | --- | --- |
| 首页总览 | 展示当前车辆及全局重要摘要 | 最近花了多少、花在哪里、车辆近况如何？ |
| 用车记账 | 快速新增不同场景的费用或补能记录 | 这次用车产生了什么记录？ |
| 费用日历 | 按日期回顾费用 | 哪一天发生了哪些费用？ |
| 数据分析 | 从趋势、类别和车辆角度分析成本 | 费用如何变化，哪类或哪辆车成本更高？ |
| 详细记录 | 查找、查看和管理完整历史记录 | 某笔记录的完整内容是什么？ |
| 能耗统计 | 统计油耗、电耗和能源成本 | 车辆实际能耗和每公里能源成本是多少？ |
| 车辆管理 | 管理多辆车及车辆基础资料 | 当前管理哪些车，每辆车的基本情况如何？ |

## 5. 各模块第一版最必要的数据和操作

### 5.1 首页总览

**必要数据**

- 当前车辆或全部车辆状态；
- 本月费用、记录数、平均每笔费用；
- 单公里成本；
- 费用趋势和费用类别汇总；
- 最近记录；
- 对应能源类型的能耗摘要。

**必要操作**

- 切换车辆；
- 点击概要进入已带筛选条件的对应模块；
- 查看最近记录详情；
- 快速记账；
- 进入全部记录或能耗统计。

### 5.2 用车记账

**支持的第一版费用类别**

- 加油；
- 充电；
- 停车；
- 洗车；
- 保养；
- 维修；
- 保险；
- 年检；
- 高速及路桥费；
- 违章罚款；
- 汽车用品；
- 其他费用。

**所有记录的通用必要数据**

- 唯一编号；
- 所属车辆；
- 费用类别；
- 金额；
- 发生日期和时间；
- 当前里程，可在不适用时留空；
- 商家或地点，可选；
- 备注，可选；
- 创建时间和最后修改时间。

**加油记录的必要专用数据**

- 加油量（升）；
- 油品标号，可选；使用下拉选择，第一版提供 92 号、95 号、98 号、0 号柴油和“其他”；
- 单价，可由总金额和加油量计算；
- 是否加满；
- 当前里程。

**充电记录的必要专用数据**

- 充电量（千瓦时）；
- 充电方式，可选；
- 单价，可由总金额和充电量计算；
- 是否充满；
- 当前里程。

**必要操作**

- 先选择记账场景，再显示对应字段；
- 新增并保存记录；
- 根据数量和单价自动计算金额，允许用户修改；
- 保存成功后选择继续记账或查看记录；
- 校验金额、数量不能为负数；
- 里程低于该车已有最高里程时提示检查，但允许用户确认后保存。

### 5.3 费用日历

**必要数据**

- 当前月份；
- 每天的记录数量和费用合计；
- 当月费用合计；
- 选中日期的记录明细；
- 当前车辆条件。

**必要操作**

- 切换上月、下月和回到本月；
- 切换车辆；
- 点击日期查看当天记录；
- 从选中日期快速新增记录；
- 点击当天记录查看、编辑或删除。

### 5.4 数据分析

**必要数据**

- 选定时间范围内的总费用；
- 按月或适合当前时间跨度的费用趋势；
- 各费用类别金额和占比；
- 不同月份的费用对比；
- 全部车辆模式下的车辆费用对比；
- 单车模式下的每公里成本；
- 平均每月费用。

**必要操作**

- 选择本月、近三个月、今年或自定义时间；
- 选择车辆或全部车辆；
- 选择全部或指定费用类别；
- 点击月份或类别进入相应的详细记录结果；
- 在数据不足时展示原因，不生成错误指标。

### 5.5 详细记录

**必要数据**

- 日期和时间；
- 所属车辆；
- 费用类别；
- 金额；
- 里程；
- 专用补能字段；
- 商家或地点；
- 备注；
- 当前筛选结果的记录数和金额合计。

**必要操作**

- 按车辆、日期、费用类别和金额范围筛选；
- 按关键词搜索；
- 按时间或金额排序；
- 默认按发生日期正序（最早优先）排列，并可切换为日期倒序（最新优先）或金额排序；
- 查看记录详情；
- 编辑记录；
- 删除记录；
- 复制一条记录作为新记录，保留车辆和类别并将时间更新为当前时间；
- 清除筛选时，清空关键词、类别、日期和金额等全部筛选条件，并恢复为“最早优先”排序；
- 使用分页或“加载更多”查看大量记录。

### 5.6 能耗统计

**必要数据**

- 具体车辆；
- 车辆能源类型；
- 带有效里程的加油或充电记录；
- 区间行驶里程；
- 区间加油量或充电量；
- 百公里油耗或百公里电耗；
- 每公里能源成本；
- 油价或电价变化；
- 是否排除异常记录。

**分车型显示**

- 燃油车：平均油耗、最近油耗、每公里燃油成本、油耗趋势、油价趋势和加油记录；
- 纯电动车：平均电耗、最近电耗、每公里充电成本、电耗趋势、电价趋势和充电记录；
- 插电式混动车：油耗、电耗、燃油费用、充电费用和综合每公里能源成本，油耗与电耗不直接相加。

**必要操作**

- 选择一辆具体车辆；
- 查看能耗趋势及补能记录；
- 点击趋势数据查看对应记录和计算依据；
- 将异常记录标记为“不参与能耗计算”；
- 数据不足时提示至少需要两条包含有效里程的相关记录。

第一版的能耗计算以相邻有效记录之间的里程差和补能量为依据。仅在数据条件满足时计算；不能把未知结果显示成 0。

### 5.7 车辆管理

**车辆必要数据**

- 唯一编号；
- 车辆名称；
- 能源类型：燃油、纯电、插电式混动；
- 初始里程；
- 当前最高有效里程；
- 是否为默认车辆；
- 创建时间和最后修改时间。

**车辆可选数据**

- 品牌及型号；
- 车牌号；
- 购车日期；
- 购车价格；
- 油箱容量；
- 电池容量。

**必要操作**

- 新增、查看和编辑车辆；
- 设为默认车辆；
- 查看每辆车的累计费用、记录数、当前里程和平均能耗；
- 删除车辆；
- 删除存在历史记录的车辆时，明确提示将连同相关记录一起删除；
- 不允许删除车辆后留下无法归属的孤立记录。

## 6. 首页和各模块之间的关系

### 6.1 全局车辆条件

- 顶部选择的当前车辆是首页、费用日历、数据分析和详细记录的默认车辆条件；
- 在上述页面切换车辆后，其他页面再次打开时应保持一致；
- 选择“全部车辆”后，首页和数据分析可展示跨车辆汇总；
- 能耗统计必须落到具体车辆，不能汇总不同能源类型车辆的能耗数值。

### 6.2 新增记录后的联动

记录保存成功后，相关页面数据必须立即更新，包括：

- 首页费用、次数、最近记录和摘要；
- 费用日历对应日期及当月合计；
- 数据分析趋势、类别占比和车辆对比；
- 详细记录列表及筛选合计；
- 符合计算条件时的能耗统计。

### 6.3 从首页进入模块

| 首页内容 | 点击后的目标 |
| --- | --- |
| 本月费用或记录数 | 详细记录，自动筛选当前车辆和本月 |
| 某个费用类别 | 详细记录，自动筛选当前车辆、当前月份和该类别 |
| 最近记录 | 打开该记录的详情抽屉 |
| 费用趋势中的月份 | 详细记录，自动筛选对应月份 |
| 能耗摘要 | 当前车辆的能耗统计 |
| “记一笔” | 用车记账，并默认带入当前车辆 |
| “查看全部记录” | 详细记录，并保留当前车辆条件 |

### 6.4 编辑和删除后的联动

- 修改金额、日期、车辆、类别、里程、加油量或充电量后，所有相关统计必须重新计算；
- 删除记录后，该记录必须从首页、日历、分析、详细记录和能耗统计中同时消失；
- 页面应保留用户可继续操作的上下文，不能要求刷新浏览器才能看到结果。

## 7. 本地数据文件、备份和恢复要求

### 7.1 日常本地存储

- App 的工作数据保存在浏览器提供的本地持久化数据库中，推荐使用 IndexedDB；
- 浏览器之间的本地存储彼此隔离：在另一浏览器或浏览器用户配置文件中打开 App 时，不会自动看到原浏览器的数据；需要通过导出备份、导入恢复迁移数据；
- 不以浏览器临时内存作为唯一数据源；
- 每次新增、修改或删除后应立即持久化；
- 刷新页面、关闭浏览器、重新打开 App 或重启电脑后，数据保持不变；
- 数据不得自动上传到网络；
- App 应显示保存状态，并在保存失败时明确提示用户；
- 浏览器数据被用户主动清理或浏览器环境损坏不属于日常持久化保证范围，因此必须提供文件备份和恢复能力。

### 7.2 备份文件

- 用户可主动导出一份完整备份文件到电脑；
- 第一版备份文件采用 UTF-8 JSON 格式；
- 建议文件名：`car-expense-backup-YYYYMMDD-HHmmss.json`；
- 备份必须包含：格式版本、导出时间、车辆、全部记录、必要设置和数据标识；
- 备份不依赖网络或云服务；
- 导出后不删除或改变当前数据；
- 同一份有效备份应可以在清空后的 App 中恢复出相同车辆、记录和必要设置。

建议的备份顶层结构：

```text
schemaVersion
exportedAt
vehicles
records
settings
```

### 7.3 导入检查

导入前必须检查：

- 文件是否能正常读取；
- 是否为 App 支持的备份格式；
- 格式版本是否兼容；
- 必要数据字段是否存在；
- 车辆与记录的归属关系是否有效；
- 数据类型是否正确；
- 文件损坏或格式错误时不得写入任何数据。

导入确认前应向用户展示：备份时间、车辆数量、记录数量和导入模式。

### 7.4 恢复方式

第一版提供两种恢复方式：

- **合并导入**：保留当前数据，根据稳定唯一编号导入备份中不存在的数据，并用备份内容更新编号相同的数据；
- **覆盖恢复**：先用有效备份替换现有业务数据，再重新加载全部统计结果。

覆盖恢复属于高风险操作，必须二次确认。校验失败或导入过程中发生错误时，现有数据必须保持原样，不能出现只导入一部分的状态。

### 7.5 数据一致性

- 每条记录必须归属于一辆存在的车辆；
- 删除车辆时必须同时处理该车辆的全部记录；
- 金额、里程、加油量和充电量等数值以原始输入值保存，展示时再按单位格式化；
- 导入、编辑或删除后，首页和各统计模块应使用同一份最新数据重新计算；
- 备份格式必须包含版本号，便于后续版本兼容和迁移。

## 8. 第一版必须完成的功能

1. 支持创建、编辑、切换、设为默认和删除多辆车。
2. 支持燃油车、纯电动车和插电式混动车。
3. 支持确认范围内的十二类用车费用。
4. 支持普通费用、加油和充电三类差异化录入字段。
5. 支持新增、查看、编辑、删除和复制记录。
6. 首页展示当前车辆、记录次数、平均每笔费用、最近记录及重要模块摘要。
7. 费用日历按日期显示费用，并可查看当天记录。
8. 数据分析支持时间、车辆和类别筛选，以及趋势、类别和车辆对比。
9. 详细记录支持筛选、搜索、排序和合计。
10. 能耗统计按车辆能源类型展示油耗、电耗或混动车的分项能耗和综合能源成本。
11. 记录发生变化后，首页、日历、分析、记录和能耗结果自动联动更新。
12. 数据在刷新、关闭浏览器和重启电脑后保持存在。
13. 支持导出完整 JSON 备份。
14. 支持校验备份并以合并或覆盖方式恢复。
15. 提供空状态、数据不足提示、表单校验、删除确认和保存状态反馈。
16. 左侧七个一级导航均显示与模块匹配的图标，并保留清晰的当前页状态。
17. 加油记录的油品标号支持下拉选择常用油品。
18. 详细记录默认最早优先排序，且清除筛选后恢复该默认排序。

## 9. 第一版暂时不做的功能

- 用户注册、登录和多账号；
- 云端数据库、多设备同步和云备份；
- 手机 App 和专门的移动端适配；
- 服务器、联网功能和正式部署；
- 社区、排行榜、记录分享和社交功能；
- 地图、行程轨迹和定位；
- 自动读取车辆、OBD 或车机数据；
- 自动连接加油站、充电平台、银行卡或支付账单；
- 发票识别、图片上传和 OCR；
- 保险、保养、年检等到期提醒；
- 预算管理、超支通知和消息推送；
- 多币种、英制单位和自定义计量单位；
- 多人共同管理同一辆车；
- Excel、CSV、PDF 等业务报表导出；第一版仅提供完整 JSON 数据备份；
- 自定义费用类别和复杂标签系统；
- 智能建议、预测费用或 AI 分析；
- 对油耗和电耗做不科学的直接合并。

## 10. 可实际检查的验收标准

以下验收均应在不登录、不联网的电脑浏览器环境中完成。

| 编号 | 验收项目 | 可检查的通过条件 |
| --- | --- | --- |
| AC-01 | 页面框架 | 打开 App 后可看到七个一级导航入口、车辆选择器、“记一笔”和“数据管理”入口，且七个页面均可进入。 |
| AC-02 | 多车辆管理 | 可分别创建一辆燃油车、一辆纯电动车和一辆插电式混动车；三辆车可独立编辑、切换并展示各自记录。 |
| AC-03 | 默认车辆 | 将任意车辆设为默认后重新打开 App，顶部自动选中该车辆。 |
| AC-04 | 普通费用记账 | 可为指定车辆新增一笔停车费；保存后详情中能正确看到车辆、类别、金额、日期和备注。 |
| AC-05 | 加油记账 | 可保存包含金额、加油量、当前里程、是否加满和油品标号的记录；油品标号可从 92 号、95 号、98 号、0 号柴油和“其他”下拉选择；数量和单价填写后可自动得到金额。 |
| AC-06 | 充电记账 | 可保存包含金额、充电量、当前里程和是否充满的记录；数量和单价填写后可自动得到金额。 |
| AC-07 | 场景化表单 | 选择停车时不强制填写加油量或充电量；选择加油或充电时能看到各自专用字段。 |
| AC-08 | 表单校验 | 金额、加油量或充电量为负数时不能保存，并显示具体错误；里程低于历史最高里程时出现提示且允许确认保存。 |
| AC-09 | 首页联动 | 新增记录后无需刷新，首页的本月费用、记录数和最近记录立即更新且数值正确。 |
| AC-10 | 首页跳转 | 点击首页费用类别后进入详细记录，并自动应用当前车辆、当前月份及该类别筛选。 |
| AC-11 | 费用日历 | 新增一笔指定日期的费用后，该日期显示正确金额；点击该日期可看到对应记录。 |
| AC-12 | 数据分析 | 选择时间、车辆和类别后，趋势、类别汇总、总费用及记录数据一致；点击月份或类别可进入对应记录。 |
| AC-13 | 详细记录筛选 | 按车辆、日期、类别和金额筛选后，只显示符合条件的记录，页面显示的数量和金额合计与记录相符。 |
| AC-14 | 搜索与排序 | 输入商家、地点或备注关键词可找到对应记录；按时间或金额排序后顺序正确。 |
| AC-15 | 查看与编辑 | 从详细记录打开一条记录，在右侧查看和编辑；保存后返回原列表位置，修改内容及相关统计立即更新。 |
| AC-16 | 删除联动 | 删除一条记录并确认后，该记录同时从首页、日历、分析、详细记录和相关能耗计算中消失。 |
| AC-17 | 能耗分车型 | 选择燃油车时显示油耗字段，选择纯电动车时显示电耗字段，选择插混车时分别显示油耗和电耗。 |
| AC-18 | 能耗数据不足 | 只有一条有效补能记录时不显示 0 油耗或 0 电耗，而是明确提示数据不足。 |
| AC-19 | 能耗计算 | 为同一车辆建立至少两条里程递增的有效补能记录后，系统能展示区间里程、计算依据、百公里能耗和每公里能源成本。 |
| AC-20 | 异常记录排除 | 将一条补能记录标记为“不参与能耗计算”后，统计结果重新计算，并可取消排除。 |
| AC-21 | 全部车辆视角 | 选择“全部车辆”后，首页和数据分析能显示跨车辆汇总；进入能耗统计时要求选择具体车辆。 |
| AC-22 | 车辆删除保护 | 删除有历史记录的车辆时，界面明确告知相关记录会一并删除；取消时数据不变，确认后车辆和相关记录均消失。 |
| AC-23 | 刷新持久化 | 新增车辆和记录后刷新浏览器，数据仍然存在且内容一致。 |
| AC-24 | 重启持久化 | 关闭浏览器并重新打开 App，或重启电脑后再次打开，已保存数据仍然存在。 |
| AC-25 | 保存反馈 | 新增或编辑时可看到保存成功状态；模拟保存失败时能看到失败提示，不能误报为已保存。 |
| AC-26 | 导出备份 | 导出后得到可读取的 JSON 文件，文件包含格式版本、导出时间、全部车辆、全部记录和必要设置，且当前数据不受影响。 |
| AC-27 | 无效备份保护 | 选择损坏文件、普通 JSON 或缺少必要字段的文件时，系统拒绝导入、说明原因，现有数据完全不变。 |
| AC-28 | 合并导入 | 在现有数据上合并一份有效备份后，不同编号的数据被加入，相同编号的数据按备份更新，且不产生重复记录。 |
| AC-29 | 覆盖恢复 | 覆盖前出现二次确认；确认后当前业务数据被备份数据完整替换，车辆数、记录数和关键内容与备份一致。 |
| AC-30 | 导入原子性 | 导入过程中发生校验或写入错误时，不出现只恢复部分车辆或部分记录的状态，原有数据仍可正常使用。 |
| AC-31 | 空状态 | 无车辆、无记录、筛选无结果和能耗数据不足时，分别显示与当前场景相符的说明及下一步操作。 |
| AC-32 | 本地运行边界 | 断开网络后，车辆管理、记账、日历、分析、记录、能耗、导出和导入仍能正常使用，不请求登录或云服务。 |
| AC-33 | 导航识别 | 左侧七个一级导航均有与模块匹配的图标，当前页面的图标、文字和状态清晰可辨。 |
| AC-34 | 记录默认排序与重置 | 进入详细记录时按最早发生日期优先排列；切换为其他排序及设置任意筛选后，点击“清除筛选”会清空全部筛选条件并恢复“最早优先”。 |
| AC-35 | 跨浏览器迁移边界 | 在另一浏览器打开 App 时不要求自动共享数据；通过导出原浏览器备份并在另一浏览器导入后，车辆、记录和必要设置可恢复。 |

## 11. 第一版核心流程

### 11.1 首次使用

1. 打开 App；
2. 创建第一辆车；
3. 自动将该车设为默认车辆；
4. 进入首页空状态；
5. 点击“记一笔”；
6. 选择记账场景并填写表单；
7. 保存后返回首页或查看记录；
8. 首页和各模块自动显示最新数据。

### 11.2 日常记账

1. 在顶部确认或切换当前车辆；
2. 点击“记一笔”；
3. 选择费用类别；
4. 填写当前场景需要的数据；
5. 保存；
6. 在首页、日历、详细记录或分析中查看结果。

### 11.3 备份与恢复

1. 用户进入“数据管理”；
2. 导出完整备份文件并自行妥善保存；
3. 需要恢复时选择备份文件；
4. App 校验并展示备份摘要；
5. 用户选择合并导入或覆盖恢复；
6. 覆盖恢复时二次确认；
7. 成功后重新加载全部车辆、记录和统计，并显示恢复结果。

---

本 PRD 是第一版设计和开发的范围依据。后续如增加功能，应先更新本文档的范围、数据要求和验收标准，再进入实现。
