# 爱车小精灵 App 交接文档

> 文档版本：V1.4
> 最后更新：2026-09-12
> 最近已同步功能提交：`59f19fb feat: 优化详细记录渐进展示`
> 当前分支：`main`；README / HANDOFF V1.4 文档更新待用户确认后另行提交

## 版本变更记录

| 版本 | 日期 | 变更内容 |
| --- | --- | --- |
| V1.0 | 2026-09-03 | 创建初版交接文档。 |
| V1.1 | 2026-09-08 | 同步 PRD V1.9 正式视觉实现与 Release 风险。 |
| V1.2 | 2026-09-09 | 同步 PRD V1.10“更多”模块与移动端导航规则。 |
| V1.3 | 2026-09-10 | 同步 PRD V1.11“更多”模块精简与断点切换规则。 |
| V1.4 | 2026-09-12 | 同步 PRD V1.12～V1.16：数据分析卡片入口、首页趋势收纳、详细记录渐进展示、最新测试与 GitHub 同步状态。 |

## 1. 我们正在做什么

“爱车小精灵”是面向个人车主的多车辆用车费用记录与分析应用。当前仓库是 React + TypeScript + Vite 构建的本地响应式 Web 应用：数据保存在浏览器 IndexedDB，可在电脑和手机浏览器验收。

长期目标是进入 TestFlight 并正式发布至 Apple App Store。用户已明确要求后续所有重要修改以 Production Ready、GitHub 安全、数据兼容、隐私、Release Build 与 App Store 审核为约束。但仓库**尚无 Xcode、Swift、原生 iOS 工程、Signing、Bundle ID 或 TestFlight 配置**；当前 Web 构建不能直接上传 App Store。

产品需求的唯一基线是 `PRD.md` **V1.16**。任何新的产品需求都必须先让用户确认，再更新 PRD 的版本、变更记录与验收标准；未经确认不得直接开始实现。

## 2. 当前代码与产品状态

- GitHub：<https://github.com/battledao/car-expense-app>
- 本地目录：`E:\05.AI Agent\06.CodeX_项目\01.DAO_爱车小精灵App`
- 当前提交：`59f19fb feat: 优化详细记录渐进展示`
- 主要技术：React、TypeScript、Vite、React Router、Dexie / IndexedDB、Zod、Recharts、Phosphor Icons。
- 无 Backend、云数据库、用户账号、网络 API、环境变量或第三方线上服务。
- 本轮未修改 IndexedDB schema、备份格式、权限、隐私、Signing、Entitlements 或 Secret 管理。

已完成的重要产品状态：

1. **V1.9 正式视觉系统**：浅雾白背景、青绿色主操作、圆角白色卡片、桌面侧边导航与手机五项底部导航；首页、记账、数据分析、车辆管理均遵循已确认视觉基准。
2. **V1.11“更多”任务中枢**：手机端从“车辆与用车”“数据与备份”任务分组开始；桌面与手机在 `767px` 断点双向切换车辆管理／更多主页，子页面有正确的“返回更多”路径。
3. **V1.13～V1.14 数据分析**：首页只保留核心指标和三张入口卡片；完整费用趋势、月份费用对比、费用类别构成分别进入 `/analysis/trend`、`/analysis/months`、`/analysis/categories`，并继承筛选条件与支持记录下钻。
4. **V1.15 首页收纳**：移除与数据分析重复的近六个月趋势，首页保留当月概览和费用构成。
5. **V1.16 详细记录渐进展示**：默认“最新优先”、首次显示 10 条／每次加载 10 条；常驻搜索、排序摘要与筛选入口；完整条件在“筛选与排序”抽屉；紧凑结果概览；桌面语义表格与手机单列卡片；编辑、复制、删除仅在详情抽屉中提供；支持超出首批记录的定位高亮。
6. **V1.16 空状态与可访问性**：区分无车辆、有车无记录、当前筛选无结果；各自提供正确恢复入口。筛选／详情／编辑／复制抽屉支持 Escape、关闭后焦点返回以及 Tab 焦点循环。

## 3. 本轮已完成什么

本轮对应 PRD V1.16，已获用户验收并提交、推送：

- 更新 `PRD.md` 至 V1.16，明确详细记录的渐进展示、筛选抽屉、默认排序、加载更多、详情内操作、三类空状态、响应式与可访问性验收。
- 改造 `src/App.tsx`、`src/styles.css`，实现上述详细记录体验与 `767px` 断点下的卡片展示。
- 更新并新增单元、组件、端到端测试；没有删除或降低任何已有测试标准。
- 将一个已不符合 V1.16 的旧空状态断言迁移为新的“无车辆→新增车辆”覆盖；这不是删除测试。
- 最终验证结果：
  - `npm run test`：6 个文件、77 项通过。
  - `npm run test:e2e`：41 项通过，覆盖桌面、iPhone、Galaxy、手机横竖屏、`767px` 断点、浏览器返回与空状态。
  - `npm run build`：通过。
  - `git diff --check` 与差异敏感信息扫描：通过。
- 提交并推送：`59f19fb feat: 优化详细记录渐进展示`。

## 4. 当前卡点、风险与影响

**没有功能性卡点。** `59f19fb` 功能提交已与 `origin/main` 同步；当前工作区仅有本次获授权更新的 `README.md` 与 `HANDOFF.md`，待用户确认后再另行提交，不要自行提交。

仍需主动关注的 Release 风险：

- 当前仍为 Web 项目，不能生成 iOS Archive 或 TestFlight Build。原生技术路线、数据迁移、环境隔离、Bundle ID、Signing、Entitlements、Privacy Manifest、Crash Reporting 与部署文档均未建立。
- `npm run build` 可通过，但 Vite 对主 JavaScript 包约 **908 kB**（gzip 约 268 kB）给出超过 500 kB 的警告。这不是本轮功能 Blocker；若要优化，先评估路由分包／Recharts 按需加载、性能收益和回归风险，未经评估不要直接大改。
- IndexedDB 仅在同一浏览器／配置文件／设备内持久化。跨浏览器或设备必须通过 JSON 备份导出和导入，不要把它误当云同步。

## 5. 下一步计划

1. 等待用户提出下一项已确认的产品或视觉需求；先评估影响并输出需求／计划，用户确认后再写代码。
2. 若接到 Bug，先增加可复现的有效测试，定位根因，跑专项测试后再继续；最终必须跑 `npm run test`、`npm run test:e2e` 和 `npm run build`。
3. 若用户要进入 TestFlight／App Store 阶段，先单独完成原生 iOS 技术路线与 Release Impact Analysis，不能把现有 Vite 产物当作可上传的 iOS Build。
4. 如用户要求性能优化，再针对主包体积完成影响评估与最小化分包方案；不在普通功能迭代中顺带重构。

## 6. 运行、测试与验收命令

在项目根目录执行：

```powershell
npm ci
npm run dev
npm run dev:mobile
npm run test
npm run test:e2e
npm run build
```

- 本地 Web：`http://127.0.0.1:5173`
- 手机局域网验收：使用 `npm run dev:mobile`，默认端口 `5174`；只限可信局域网，验收后停止服务。
- 浏览器自动化不可用或不稳定时，优先使用仓库 Playwright 测试作为可重复的验证回退，不要将工具不可用误判为产品缺陷。

## 7. 绝对不要再踩的坑

1. **先确认、后实现**：用户未确认需求时，只输出需求或计划；不得提前改业务代码。PRD 改动必须升版本。
2. **测试不能取巧**：不得删除、跳过、弱化测试或降低验收标准。若测试确实不再符合需求，必须先列出“拟删除的测试、失效原因、替代覆盖”，得到用户明确授权后才可删除；优先迁移断言并保留覆盖。
3. **详细记录不要退回旧布局**：不要把全部筛选字段、平均单笔卡片或编辑／复制／删除按钮重新平铺到首屏。默认排序是“最新优先”，不是“最早优先”。
4. **URL 历史语义不要破坏**：筛选抽屉“查看 N 条记录”应产生可返回的状态；关键词逐字输入和“清除筛选”使用替换历史以避免历史噪声。`tests/e2e/records.spec.ts` 已覆盖这两类行为。
5. **手机断点不能只看 CSS**：`767px` 是“更多”与车辆管理页面归属、详细记录卡片／表格的关键断点。每次改响应式路由或布局都要测试从桌面缩小到手机、再放大回桌面的双向状态。
6. **不要误操作数据与 Git 历史**：禁止 `git reset --hard`、`git checkout --`、强制推送或未经评估的批量删除。用户验收并授权前不能提交；GitHub 推送需要用户明确授权目标远端和分支。
7. **不要提交敏感信息**：`.env`、Token、Password、API Key、私钥、证书、真实备份、用户隐私数据、含敏感内容的截图或日志均不得进入 Git 或文档。每次提交前运行 `git diff --check` 并扫描差异。
8. **不要把 Web 验收误认为 iOS 发布准备完成**：本项目现阶段没有 iOS 原生工程、环境隔离、签名与隐私配置；涉及这些项目的修改必须先做影响分析。
9. **Windows 换行提示不是失败**：Git 的 LF→CRLF 警告是提示；仍要看实际退出码和 `git diff --check` 结果，不能因此跳过验证。

## 8. 文档维护规则

- 本次已在用户明确授权下更新 `README.md` 和本文件，并将二者递增至 V1.4。
- 后续如判断 README 或 HANDOFF 需要更新，先提醒并询问用户是否授权更新；获得授权后才修改，并递增版本号与记录变更。
- 文档只记录公开、安全、可复现的信息；不得记录 Secret、真实用户数据或内部私密配置。