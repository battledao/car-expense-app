# 爱车小精灵 App 交接文档

> 文档版本：V1.2
> 变更规则：每次更新交接内容时，递增版本号，并在下表记录修改内容。

## 版本变更记录

| 版本 | 日期 | 变更内容 |
| --- | --- | --- |
| V1.0 | 2026-09-03 | 创建初版交接文档，记录项目状态、开发成果、测试方式和注意事项。 |
| V1.1 | 2026-09-08 | 同步 PRD V1.9 UI 正式视觉实现、测试基线、Release 风险与后续验收事项。 |
| V1.2 | 2026-09-09 | 同步 PRD V1.10“更多”模块实现、移动端导航语义、响应式覆盖和最新测试基线。 |

> 最后更新：2026-09-09
> 项目目录：`E:\05.AI Agent\06.CodeX_项目\01.DAO_爱车小精灵App`
> GitHub：<https://github.com/battledao/car-expense-app>
> 当前分支：`main`（已与 `origin/main` 同步）

## 1. 我们正在做什么

这是一个仅在电脑浏览器本地运行的车主用车记账 App，产品名为“爱车小精灵”。目标是让个人车主离线管理多辆车、记录各类用车费用、查看费用趋势和能耗数据，并可通过 JSON 导出备份、导入恢复数据。

正式需求以 `PRD.md` V1.10 为准。首页、用车记账、数据分析与车辆管理已按四张确认视觉稿共同确定为正式视觉基准；“更多”已按同一设计系统实现任务中枢。后续任何产品需求修改都必须先取得用户明确同意，再修改 `PRD.md`；每次修改 PRD 都要升版本，并用 `【Vx.x 新增】`或`【Vx.x 修改】`标记变动。

## 2. 已完成的产品功能

- 多车辆管理：新增、编辑、删除、默认车辆、燃油车／纯电车／插混车。
- 用车记账：停车、洗车、高速及路桥费、违章罚款、保养、维修、保险、年检、汽车用品、其他、加油、充电等场景。
- 车型适配：燃油车仅加油、纯电车仅充电、插混车同时支持加油和充电。
- 用车记账重构（PRD V1.4）：车辆上下文、场景选择、补能字段、油品标号／充电方式、金额单价自动计算、校验、保存反馈、保存并继续记账等。
- 首页总览、费用日历、数据分析、详细记录、能耗统计、车辆管理、数据管理。
- 详细记录：默认最早优先排序，可切换排序；“清除筛选”会还原为最早优先。
- 能耗统计：油耗、电耗、插混综合能源成本、完整区间、数据质量、趋势及补能记录管理。
- 本地数据：使用浏览器 IndexedDB（Dexie），刷新和重启同一浏览器后保留；不同浏览器不会自动共享数据，需使用导出／导入迁移。
- 数据备份：完整 JSON 导出，导入前校验，支持合并和覆盖恢复。
- Windows 启动脚本：`启动运行App.bat` 与 `暂停运行App.bat` 已处理过路径及停止逻辑问题。
- V1.9 UI：桌面端七项图标侧边导航、手机端五项图标底部导航与“更多”入口；首页月度费用主卡及本地装饰插画；用车记账的专注填写页；数据分析摘要、筛选和下钻卡片；车辆摘要与安全操作卡片。
- V1.10“更多”：移除旧的快捷记账入口，改为“车辆与用车”“数据与安全”两个分组和三张任务卡；车辆数量、默认车辆与能耗提示均从当前本地状态派生；移动端在“更多”、车辆、能耗和数据管理路径保持“更多”导航项的视觉与 `aria-current` 选中态。

## 3. 最近完成的界面工作

V1.9 正式视觉改造已完成以下范围：

- 建立浅雾灰白背景、青绿色主操作、白色圆角卡片和柔和阴影等统一视觉令牌。
- 新增 `@phosphor-icons/react`，替代主导航的文字或 Emoji 型图标；依赖为 MIT 许可证且未增加系统权限。
- 新增 `src/assets/dashboard-car-city.webp` 本地装饰资源，不含文字、用户资料或敏感信息。
- 增加 `tests/e2e/v19-shell.spec.ts`：覆盖 390px 手机视口的主操作、底部导航、当前状态和横向溢出，以及 1440px 桌面侧边导航可见性。

V1.10“更多”模块已完成以下范围：

- 使用现有浅雾白／青绿色设计令牌，未增加视觉依赖、图片资源或第三方服务。
- 增加 `src/App.test.tsx` 的车辆上下文、空状态和“全部车辆”能耗提示单元测试。
- 新增 `tests/e2e/more.spec.ts`：覆盖 390px 移动布局、三张卡片、跳转与浏览器返回路径、子页面选中态，以及 320、375、390、414、430、768、1024、1280px 无横向溢出。

最新已同步提交：

- `29f3ec8 fix: 统一用车记账字段字体`
- `bc46a5b fix: 统一用车记账表单高度`
- `ee62409 fix: 修复用车记账字段重叠`
- `1281df1 feat: 优化用车记账模块`

## 4. 当前状态与卡点

V1.10 实现、专项测试、全量测试和生产构建均已完成，当前等待用户最终视觉验收；不得自行提交或推送。工作区存在本轮 UI、测试、PRD 及文档修改。未引入数据库迁移、网络依赖、权限、Signing、Entitlements 或 Secret。

Production Build 已可通过；Vite 报告主 JavaScript chunk 大于 500KB 的性能警告，当前不阻断本地 Web 验收，但在建立 TestFlight/原生发布工程前应评估代码分割、图表库按需加载及包体预算。

此前曾多次因本机网络无法连接 `github.com:443` 而推送失败；最后一次核对前已成功推送。若后续推送失败，先检查网络／代理，再重试，不要重写历史或执行强制推送。

## 5. 推荐的下一步计划

1. 用户在 390px 手机和桌面宽度验收 V1.10“更多”页；重点检查双分组、三张卡片、真实车辆状态、空状态、底部导航和返回路径。
2. 收到视觉或业务问题后，先补充可复现的有效测试，使用最小改动修复根因，并先跑专项、后跑全量测试与构建。
3. 用户明确说“验收完成”后，才创建 Git 提交并推送 GitHub。
4. TestFlight/原生开发启动前，先完成 iOS 技术路线、环境隔离、数据迁移、Bundle ID、Signing、Privacy Manifest、构建产物与 500KB 主包警告的影响评估。

## 6. 测试与运行命令

在项目根目录运行：

```powershell
npm run dev
npm run test
npm run test:e2e
npm run build
```

验收规则：一个问题完成后至少运行新增的专项测试；用户验收前还应运行 `npm run test`、`npm run test:e2e` 和 `npm run build`。V1.10 完工基线：Vitest 6 个文件、73 项通过；Playwright 30 项通过；Production Build 通过。不得删除、跳过或降低测试标准。

## 7. 绝对不要再踩的坑

- 不要未经用户明确授权修改 `PRD.md`，不要擅自扩展需求或改动其它模块。
- 不要把浏览器 IndexedDB 当作跨浏览器共享存储；切换浏览器必须走导出／导入。
- 不要删除、跳过或弱化测试来获得通过；必须用测试验证实际修复。
- 不要在用户验收前自行提交或推送代码。
- 不要使用 `git reset --hard`、`git checkout --`、强制推送或其他可能丢失用户数据的 Git 操作。
- Git 需带安全目录配置：`git -c safe.directory='E:/05.AI Agent/06.CodeX_项目/01.DAO_爱车小精灵App' ...`。
- Windows 上 Git 可能提示 LF 将转换为 CRLF；这只是换行符提示，不是失败。
- `npm run build` 会出现 JavaScript bundle 大于 500kB 的 Vite 警告；构建仍成功。除非用户要求性能优化，不要因该提示引入无关改造。
- CSS 文件大部分规则压缩在单行。改表单网格时必须用实际浏览器尺寸的 Playwright `boundingBox()` 检查重叠、等高、对齐和字体，不能只凭代码推测。
- 近期表单样式位置：`src/styles.css` 的 `.record-primary` 和 `.record-extra`；布局回归测试位置：`tests/e2e/app.spec.ts` 的“keeps the primary record fields...”测试。
